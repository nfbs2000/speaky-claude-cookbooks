# 23장: 비공개 기능 파이프라인 - 기능 플래그와 재현 조건

이 페이지는 한국어 SDK 책의 `23장: 비공개 기능 파이프라인 - 기능 플래그와 재현 조건`을 공개 Python cookbook과 공식 Agent SDK 문서에 맞춰 다시 쓴 공개판이다. 원래 장의 문제의식은 유지하되, 내부 TypeScript 구현명이나 비공개 기능을 그대로 옮기지 않는다. 대신 실행 가능한 notebook, Python 파일, README, 공식 문서를 근거로 사용한다.

**분류:** 제6부: 고급 서브시스템<br>
**공개 상태:** `omit`<br>
**근거 신뢰도:** `low`<br>
**원문 위치:** `docs/book-sdk-ko/src/part6/ch23.md`

## 이 장의 공개판 요지

이 장은 공개 사이트에서 원문을 그대로 옮기지 않는다. 공개판에서는 재현 조건, 운영 원칙, 공개 API 대응만 남긴다.

공개 사이트에서는 비공개 기능 플래그 목록이나 내부 rollout pipeline을 게시하지 않는다. 이 장은 "기능 차이를 재현 가능하게 기록하는 법"으로 축약한다. 공개 릴리스 노트, 공식 what's new, cookbook notebook version/date, environment variables를 기준으로 어떤 조건에서 결과가 달라졌는지 기록한다. 실무적으로는 feature name보다 reproduction bundle이 중요하다. 모델 alias, SDK version, CLI version, notebook commit, environment, permission mode, tool list를 함께 남긴다.

[Cookbook registry with dates and categories](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/registry.yaml)를 근거로 삼는다. [Python dependencies](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/pyproject.toml)를 근거로 삼는다.

!!! evidence "주요 cookbook 근거"
    - [Cookbook registry with dates and categories](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/registry.yaml)
    - [Python dependencies](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/pyproject.toml)
    - [Repository development instructions](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/CLAUDE.md)

!!! evidence "공식 문서 근거"
    - [What's new](https://code.claude.com/docs/en/whats-new/index.md)
    - [Claude Code changelog](https://code.claude.com/docs/en/changelog.md)
    - [Environment variables](https://code.claude.com/docs/en/env-vars.md)

## 원문 절 구조를 공개 SDK로 다시 읽기

### 23.1 핵심 질문

이 절은 `비공개 기능 파이프라인 - 기능 플래그와 재현 조건`의 질문을 공개 SDK에서 관측 가능한 사건으로 좁힌다. 답은 내부 구현명이 아니라 `ClaudeAgentOptions`, message stream, tool call, session record, cookbook 실행 결과에서 찾아야 한다.

### 23.2 관찰 가능한 기능 차이

이 절은 원문의 `23.2 관찰 가능한 기능 차이` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개 사이트에서는 비공개 기능 플래그 목록이나 내부 rollout pipeline을 게시하지 않는다. 이 장은 "기능 차이를 재현 가능하게 기록하는 법"으로 축약한다. 공개 릴리스 노트, 공식 what's new, cookbook notebook version/date, environment variab... 이며, 먼저 `Cookbook registry with dates and categories`를 기준 예제로 읽는다.

### 23.3 재현 조건 기록

이 절은 원문의 `23.3 재현 조건 기록` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개 사이트에서는 비공개 기능 플래그 목록이나 내부 rollout pipeline을 게시하지 않는다. 이 장은 "기능 차이를 재현 가능하게 기록하는 법"으로 축약한다. 공개 릴리스 노트, 공식 what's new, cookbook notebook version/date, environment variab... 이며, 먼저 `Cookbook registry with dates and categories`를 기준 예제로 읽는다.

### 23.4 캔버스 표현

이 절은 화면 구성의 문제가 아니라 evidence projection 문제로 읽는다. prompt, tool use, tool result, final result, usage를 한 화면에서 분리해 보여주는 구조가 필요하다.

### 23.5 학생/강사 fallback 계약

이 절은 원문의 `23.5 학생/강사 fallback 계약` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개 사이트에서는 비공개 기능 플래그 목록이나 내부 rollout pipeline을 게시하지 않는다. 이 장은 "기능 차이를 재현 가능하게 기록하는 법"으로 축약한다. 공개 릴리스 노트, 공식 what's new, cookbook notebook version/date, environment variab... 이며, 먼저 `Cookbook registry with dates and categories`를 기준 예제로 읽는다.

### 23.6 학생 실습

이 절은 강의용 실습으로 바꾼다. 실습은 관련 notebook을 실행하고, 입력 옵션과 tool call, 중간 결과, 최종 artifact를 함께 기록하는 방식이어야 한다.

### Takeaway

이 절의 결론은 공개 근거로 다시 닫는다. 내부 설명을 암기하는 대신 어떤 SDK 표면과 cookbook 파일로 같은 주장을 확인할 수 있는지 남긴다.

## 공개판 본문

원래 책은 이 장을 내부 구현과 강의 화면의 대응 관계로 설명한다. 공개판에서는 같은 내용을 "무엇을 설정했는가", "무엇이 메시지로 관측됐는가", "어떤 파일과 notebook으로 재현 가능한가"라는 세 질문으로 바꾼다.

첫째, 설정된 것은 실행 전 계약이다. Agent SDK에서는 model, system prompt, working directory, allowed/disallowed tools, MCP servers, skills/plugins, permission mode 같은 값이 agent가 볼 수 있는 세계를 정한다. 이 값은 말로 설명하는 정책이 아니라 실제 Python 코드와 notebook cell에서 확인되어야 한다.

둘째, 관측된 것은 message stream과 artifact다. assistant text, tool use, tool result, result message, usage/cost, audit log, generated file은 모두 나중에 검증 가능한 증거다. 그래서 이 책의 공개판은 "모델이 그렇게 했을 것이다"라고 쓰지 않고, 어떤 cookbook 파일에서 어떤 실행 표면을 볼 수 있는지 연결한다.

셋째, 추론한 것은 반드시 경계와 함께 둔다. 내부 기능 플래그, 숨은 프롬프트, classifier, cache key, sandbox implementation처럼 공개 SDK나 cookbook으로 확인할 수 없는 항목은 원문을 그대로 게시하지 않는다. 대신 공개 API에서 사용자가 설계할 수 있는 대응 표면으로 바꾸거나, "공개 대응 없음"으로 명시한다.

이 장을 읽을 때는 아래 순서가 좋다.

1. 먼저 주요 cookbook 근거를 열어 실제 notebook이나 Python 파일을 확인한다.
2. `ClaudeAgentOptions`, `query()`, `ClaudeSDKClient`, tool list, MCP config, hooks, session 관련 코드가 어디 있는지 찾는다.
3. 원문 절 제목을 따라가며 내부 설명을 공개 SDK 표면으로 바꿔 적는다.
4. 마지막으로 실습 방향에 맞춰 같은 task를 실행하고 message stream 또는 artifact를 남긴다.

## 공개 경계

- 기능 플래그 전체 목록은 공개하지 않는다.
- 비공개 rollout 조건은 추정하지 않는다.

## 실습 방향

- notebook 실행 결과를 재현하려면 SDK/CLI/model/env/tool list를 함께 기록한다.

## Builder takeaway

이 장의 공개판 목표는 원문을 얕게 요약하는 것이 아니다. 책의 논지를 유지하되, 독자가 직접 열어볼 수 있는 Python cookbook과 공식 문서에 묶어 두는 것이다. 따라서 장을 읽은 뒤에는 적어도 하나의 notebook 또는 Python 파일에서 같은 개념을 확인할 수 있어야 한다. 확인할 수 없는 내부 세부는 주장으로 남기지 않고, 공개 경계나 추론으로 분리한다.

## Codex 최종 검토 의견

### 제 판단

숨은 기능 플래그의 이름을 추측하는 대신 실행 시작 시 실제로 노출된 도구·스킬·플러그인 표면과 fallback을 기록하라는 결론은 실용적이고 책임 있는 방향입니다. 학생에게 중요한 것은 내부 배포 시스템의 정체가 아니라 “지금 내 실행에서 무엇을 사용할 수 있었는가”이기 때문입니다. 다만 이 관점에서 보면 장의 실제 주제는 비공개 기능 파이프라인 자체보다 **관찰 가능한 capability manifest와 대체 경로**에 가깝습니다.

### 검증을 읽고 달라진 신뢰도

호스트가 `Read`만 구성했고 init도 `[Read]`만 보고했으며, 모델이 런타임 파일을 직접 읽어 `Agent` 없이 목표를 완료한 실행은 fallback 계약의 가능성을 뒷받침합니다. 반면 `skills=[]`와 init skills가 일치하지 않았고 최종 형식에도 설명 문장이 추가됐습니다. 무엇보다 `Agent`가 빠진 내부 이유, cohort, flag value, rollout 규칙은 어떤 이벤트에도 없으므로 비공개 파이프라인에 대한 신뢰가 올라간 것은 아닙니다.

### 독자가 오해할 위험

도구가 보이지 않는다는 사실에서 특정 기능 플래그가 꺼졌다고 역추론하면 안 됩니다. 계정, 버전, 명시적 옵션, 권한, 빌드 등 여러 원인이 같은 표면을 만들 수 있습니다. 또한 fallback이 한 번 성공했다고 원래 경로와 품질·비용·안전성이 동등하다고 볼 수 없습니다. 이 실행은 같은 학습 목표를 최소 도구로 완수할 수 있었다는 한 사례입니다.

### 제가 다시 가르친다면

“내부 원인” 열은 비워 두고 `configured`, `init observed`, `behavior`, `fallback result` 네 열만 비교하겠습니다. 도구 표면 A와 B를 새 세션에서 반복 실행해 성공률과 결과 품질까지 비교해야 fallback의 실용성을 판단할 수 있습니다. [낯선 코드베이스 탐색 노트북](https://nfbs2000.github.io/speaky-claude-cookbooks/notebooks/managed_agents/CMA_explore_unfamiliar_codebase_kr.html)은 문서 추측을 실제 코드 탐색으로 교정하는 기준이고, 현재 공개 표면은 [Speaky Agent Flow 23장 재생](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch23)에서 확인할 수 있습니다.
