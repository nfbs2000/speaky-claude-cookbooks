# 5장: 시스템 프롬프트 아키텍처

이 페이지는 한국어 SDK 책의 `5장: 시스템 프롬프트 아키텍처`을 공개 Python cookbook과 공식 Agent SDK 문서에 맞춰 다시 쓴 공개판이다. 원래 장의 문제의식은 유지하되, 내부 TypeScript 구현명이나 비공개 기능을 그대로 옮기지 않는다. 대신 실행 가능한 notebook, Python 파일, README, 공식 문서를 근거로 사용한다.

**분류:** 제2부: 프롬프트 엔지니어링<br>
**공개 상태:** `public-rewrite`<br>
**근거 신뢰도:** `medium`<br>
**원문 위치:** `docs/book-sdk-ko/src/part2/ch05.md`

## 이 장의 공개판 요지

이 장은 원문의 내부 구현 표현을 공개 SDK 옵션, 메시지, 도구, 세션, notebook 실행 증거로 바꿔 읽어야 한다.

공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-staff agent는 `CLAUDE.md`와 output style을 실제 작업 디렉터리 설정으로 로드하는 예를 제공한다. 중요한 메시지는 "프롬프트도 배포 가능한 설정"이라는 점이다. 공개 사이트에서는 우선순위와 조합 규칙을 문서화하되, 숨은 기본 프롬프트 원문을 재현하려 하지 않는다.

[Chief of staff prompt and output style examples](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/01_The_chief_of_staff_agent.ipynb)를 근거로 삼는다. [Chief of staff CLAUDE.md](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/chief_of_staff_agent/CLAUDE.md)를 근거로 삼는다.

!!! evidence "주요 cookbook 근거"
    - [Chief of staff prompt and output style examples](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/01_The_chief_of_staff_agent.ipynb)
    - [Chief of staff CLAUDE.md](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/chief_of_staff_agent/CLAUDE.md)
    - [Skills project instructions](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/skills/CLAUDE.md)

!!! evidence "공식 문서 근거"
    - [Modifying system prompts](https://code.claude.com/docs/en/agent-sdk/modifying-system-prompts.md)
    - [Use Claude Code features in the SDK](https://code.claude.com/docs/en/agent-sdk/claude-code-features.md)
    - [How Claude remembers your project](https://code.claude.com/docs/en/memory.md)

## 원문 절 구조를 공개 SDK로 다시 읽기

### 5.1 시스템 프롬프트에 아키텍처가 필요한 이유

이 절은 숨은 prompt 원문이 아니라 `system_prompt`, `CLAUDE.md`, tool description, skill instruction, output style처럼 공개적으로 조정 가능한 표면으로 바꿔 설명한다.

### 5.2 원본의 `systemPromptSection`을 SDK로 번역하기

이 절은 원문의 `5.2 원본의 `systemPromptSection`을 SDK로 번역하기` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.3 `DANGEROUS_uncachedSystemPromptSection`의 의미

이 절은 원문의 `5.3 `DANGEROUS_uncachedSystemPromptSection`의 의미` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.4 `Options.systemPrompt`의 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY`

이 절은 원문의 `5.4 `Options.systemPrompt`의 `SYSTEM_PROMPT_DYNAMIC_BOUNDARY`` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.5 `systemPrompt` prefix를 SDK 관점으로 읽기

이 절은 원문의 `5.5 `systemPrompt` prefix를 SDK 관점으로 읽기` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.6 `buildEffectiveSystemPrompt`: 우선순위는 반드시 설명해야 한다

이 절은 원문의 `5.6 `buildEffectiveSystemPrompt`: 우선순위는 반드시 설명해야 한다` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.7 직접 보이는 것과 추론해야 하는 것

이 절은 원문의 `5.7 직접 보이는 것과 추론해야 하는 것` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 시스템 프롬프트를 내부 섹션명으로 설명하지 않는다. 대신 Agent SDK의 `system_prompt`, `claude_code` preset, `append`, `CLAUDE.md`, output styles, project settings로 나눈다. cookbook의 chief-of-s... 이며, 먼저 `Chief of staff prompt and output style examples`를 기준 예제로 읽는다.

### 5.8 책 캔버스 요구사항

이 절은 화면 구성의 문제가 아니라 evidence projection 문제로 읽는다. prompt, tool use, tool result, final result, usage를 한 화면에서 분리해 보여주는 구조가 필요하다.

### 5.9 학생 실습

이 절은 강의용 실습으로 바꾼다. 실습은 관련 notebook을 실행하고, 입력 옵션과 tool call, 중간 결과, 최종 artifact를 함께 기록하는 방식이어야 한다.

### Builder takeaway

이 절의 결론은 builder checklist로 바꾼다. agent를 만들 때 prompt, tools, permissions, memory, observability, deployment 중 어느 경계를 설계해야 하는지 정리한다.

## 공개판 본문

원래 책은 이 장을 내부 구현과 강의 화면의 대응 관계로 설명한다. 공개판에서는 같은 내용을 "무엇을 설정했는가", "무엇이 메시지로 관측됐는가", "어떤 파일과 notebook으로 재현 가능한가"라는 세 질문으로 바꾼다.

첫째, 설정된 것은 실행 전 계약이다. Agent SDK에서는 model, system prompt, working directory, allowed/disallowed tools, MCP servers, skills/plugins, permission mode 같은 값이 agent가 볼 수 있는 세계를 정한다. 이 값은 말로 설명하는 정책이 아니라 실제 Python 코드와 notebook cell에서 확인되어야 한다.

둘째, 관측된 것은 message stream과 artifact다. assistant text, tool use, tool result, result message, usage/cost, audit log, generated file은 모두 나중에 검증 가능한 증거다. 그래서 이 책의 공개판은 "모델이 그렇게 했을 것이다"라고 쓰지 않고, 어떤 cookbook 파일에서 어떤 실행 표면을 볼 수 있는지 연결한다.

셋째, 추론한 것은 반드시 경계와 함께 둔다. 내부 기능 플래그, 숨은 프롬프트, classifier, cache key, sandbox implementation처럼 공개 SDK나 cookbook으로 확인할 수 없는 항목은 원문을 그대로 게시하지 않는다. 대신 공개 API에서 사용자가 설계할 수 있는 대응 표면으로 바꾸거나, "공개 대응 없음"으로 명시한다.

이 장을 읽을 때는 아래 순서가 좋다.

1. 먼저 주요 cookbook 근거를 열어 실제 notebook이나 Python 파일을 확인한다.
2. `ClaudeAgentOptions`, `query()`, `ClaudeSDKClient`, tool list, MCP config, hooks, session 관련 코드가 어디 있는지 찾는다.
3. 원문 절 제목을 따라가며 내부 설명을 공개 SDK 표면으로 바꿔 적는다.
4. 마지막으로 실습 방향에 맞춰 같은 task를 실행하고 message stream 또는 artifact를 남긴다.

## 공개 경계

- 기본 시스템 프롬프트 원문이나 내부 boundary token은 게시하지 않는다.
- 공개 옵션으로 재현 가능한 계층만 설명한다.

## 실습 방향

- `setting_sources`를 바꿔 project 설정이 로드되는지 확인한다.
- `CLAUDE.md` 수정 전후로 같은 prompt의 tool use 차이를 기록한다.

## Builder takeaway

이 장의 공개판 목표는 원문을 얕게 요약하는 것이 아니다. 책의 논지를 유지하되, 독자가 직접 열어볼 수 있는 Python cookbook과 공식 문서에 묶어 두는 것이다. 따라서 장을 읽은 뒤에는 적어도 하나의 notebook 또는 Python 파일에서 같은 개념을 확인할 수 있어야 한다. 확인할 수 없는 내부 세부는 주장으로 남기지 않고, 공개 경계나 추론으로 분리한다.

## 부록: 이 장을 실제 SDK 실행으로 확인한 결과

> 위 공개판 본문은 이 저장소의 notebook과 공식 문서에 근거해 다시 쓴 것이다. 아래는 같은
> 장의 주장을 실제 Claude Agent SDK로 실행해 관찰한 기록이며, **실측 결과로 위 본문을 고쳐
> 쓰지 않았다.** 본문 설명과 실제 동작이 어긋나는 곳도 본문을 남기고 아래에 근거와 함께 적는다.

**실행 조건** — 실제 모델 `claude-opus-5`, Claude Agent SDK `0.2.128`. 세 가지 시스템 프롬프트
설정을 따로 실행했다(`102115-1592cc3c` 직접 문자열, `102206-ff8fd397` preset + append,
`102245-348d28d3` 동적 구역 제외). 원본 사건 151건 중 48건을 정리했다.

### 실제로 확인된 것

- **설정한 것과 관찰한 것이 실제로 분리된다.** 세 실행의 시작 기록은 작업 디렉터리, 모델,
  권한 모드, 도구, 스킬, 플러그인, MCP, 에이전트 목록을 드러냈지만 **조립된 시스템 프롬프트
  전문은 어디에도 나오지 않았다.** 이 장이 강조한 경계가 그대로 관찰된다.
- 직접 문자열을 설정한 실행에서 첫 `Read`가 실패하고, 그 오류가 알려 준 작업 디렉터리를
  이용해 재시도해 성공하는 흐름이 하나의 순서로 이어졌다.
- preset + append 실행도 시작 → `Read` → 도구 결과 → 최종 설명 → 성공 종료 순서로 관찰됐다.
- 세 실행 모두 **성공한 `Read`와 그 도구 결과가 최종 답변보다 먼저** 일어났다. 답변이
  근거를 인용한 순서가 실제 기록과 맞다.
- `setting_sources=[]`로 두었는데도 시작 기록에 스킬 16개가 남아 있었다. 파일시스템 설정
  격리와 `skills=[]` 비활성화는 서로 다른 제어다.
- 언어별 표면이 다릅니다. TypeScript SDK는 문자열 배열과 동적 경계를 공개하고, Python
  `0.2.128`은 custom/preset/file과 snake_case 옵션을 공개한다.

### 본문을 이렇게 읽으면 안 되는 곳

- **"이 실행은 Opus 5만 썼다"** — 주 어시스턴트 모델은 세 실행 모두 Opus 5였지만, preset 실행
  두 건의 종료 기록에는 Haiku 4.5 보조 사용도 남았다. 사용량 전체를 한 모델로 적으면
  안 된다. (근거: `102206-ff8fd397`·`102245-348d28d3` SDK 메시지 41)
- **`exclude_dynamic_sections=True`를 "내부 프롬프트가 옮겨지는 것을 봤다"로 읽으면 안 된다.**
  확인된 것은 옵션이 설정됐고 실행이 성공했다는 사실까지다.

### 이번 실행으로는 확인하지 못한 것

- 조립 전후 프롬프트 비교와, 같은 요청을 반복했을 때의 캐시 개선 효과
- TypeScript 쪽 배열·동적 경계의 실제 실행 기록(정적 타입으로만 확인)
- 정책을 제거한 대조군이 없어서, 관찰된 행동이 프롬프트 때문이라고 단정할 수 없다.
  preset이 작업 디렉터리 처리에서 일반적으로 더 낫다는 결론도 아직 이릅니다.
