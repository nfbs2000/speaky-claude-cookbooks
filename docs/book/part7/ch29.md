# 29장: 옵저버빌리티 엔지니어링 - logEvent에서 프로덕션급 텔레메트리까지

이 페이지는 한국어 SDK 책의 `29장: 옵저버빌리티 엔지니어링 - logEvent에서 프로덕션급 텔레메트리까지`을 공개 Python cookbook과 공식 Agent SDK 문서에 맞춰 다시 쓴 공개판이다. 원래 장의 문제의식은 유지하되, 내부 TypeScript 구현명이나 비공개 기능을 그대로 옮기지 않는다. 대신 실행 가능한 notebook, Python 파일, README, 공식 문서를 근거로 사용한다.

**분류:** 제7부: AI 에이전트 구축자를 위한 교훈<br>
**공개 상태:** `public`<br>
**근거 신뢰도:** `high`<br>
**원문 위치:** `docs/book-sdk-ko/src/part7/ch29.md`

## 이 장의 공개판 요지

이 장은 공개 SDK와 cookbook 예제로 대부분 직접 설명할 수 있다.

공개판에서는 observability를 SDK message stream, OpenTelemetry, usage/cost API, audit logs, session storage로 구성한다. observability agent notebook은 MCP를 통해 외부 시스템을 읽는 agent를 보여주고, usage/cost cookbook은 운영 비용 관측을 제공한다. 좋은 telemetry는 agent가 성공했는지만이 아니라 왜 그 결론에 도달했는지, 어떤 도구를 썼는지, 어떤 근거가 부족했는지를 보여줘야 한다.

[Observability agent](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/02_The_observability_agent.ipynb)를 근거로 삼는다. [Usage and cost Admin API cookbook](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/observability/usage_cost_api.ipynb)를 근거로 삼는다.

!!! evidence "주요 cookbook 근거"
    - [Observability agent](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/02_The_observability_agent.ipynb)
    - [Usage and cost Admin API cookbook](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/observability/usage_cost_api.ipynb)
    - [Observability agent implementation](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/observability_agent/agent.py)
    - [Audit log example](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/claude_agent_sdk/chief_of_staff_agent/audit/script_usage_log.json)

!!! evidence "공식 문서 근거"
    - [Observability with OpenTelemetry](https://code.claude.com/docs/en/agent-sdk/observability.md)
    - [Track cost and usage](https://code.claude.com/docs/en/agent-sdk/cost-tracking.md)
    - [Monitoring](https://code.claude.com/docs/en/monitoring-usage.md)

## 원문 절 구조를 공개 SDK로 다시 읽기

### 29.1 핵심 질문

이 절은 `옵저버빌리티 엔지니어링 - logEvent에서 프로덕션급 텔레메트리까지`의 질문을 공개 SDK에서 관측 가능한 사건으로 좁힌다. 답은 내부 구현명이 아니라 `ClaudeAgentOptions`, message stream, tool call, session record, cookbook 실행 결과에서 찾아야 한다.

### 29.2 텔레메트리 파이프

이 절은 원문의 `29.2 텔레메트리 파이프` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 observability를 SDK message stream, OpenTelemetry, usage/cost API, audit logs, session storage로 구성한다. observability agent notebook은 MCP를 통해 외부 시스템을 읽는 agent를 보여주고, ... 이며, 먼저 `Observability agent`를 기준 예제로 읽는다.

### 29.3 최소 schema

이 절은 원문의 `29.3 최소 schema` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 observability를 SDK message stream, OpenTelemetry, usage/cost API, audit logs, session storage로 구성한다. observability agent notebook은 MCP를 통해 외부 시스템을 읽는 agent를 보여주고, ... 이며, 먼저 `Observability agent`를 기준 예제로 읽는다.

### 29.4 계층별 책임

이 절은 원문의 `29.4 계층별 책임` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 observability를 SDK message stream, OpenTelemetry, usage/cost API, audit logs, session storage로 구성한다. observability agent notebook은 MCP를 통해 외부 시스템을 읽는 agent를 보여주고, ... 이며, 먼저 `Observability agent`를 기준 예제로 읽는다.

### 29.5 Genkit/Opik을 프롬프트에 쓰는 안전한 방법

이 절은 숨은 prompt 원문이 아니라 `system_prompt`, `CLAUDE.md`, tool description, skill instruction, output style처럼 공개적으로 조정 가능한 표면으로 바꿔 설명한다.

### 29.6 PII와 교육 데이터

이 절은 원문의 `29.6 PII와 교육 데이터` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 observability를 SDK message stream, OpenTelemetry, usage/cost API, audit logs, session storage로 구성한다. observability agent notebook은 MCP를 통해 외부 시스템을 읽는 agent를 보여주고, ... 이며, 먼저 `Observability agent`를 기준 예제로 읽는다.

### 29.7 학생 실습

이 절은 강의용 실습으로 바꾼다. 실습은 관련 notebook을 실행하고, 입력 옵션과 tool call, 중간 결과, 최종 artifact를 함께 기록하는 방식이어야 한다.

### Takeaway

이 절의 결론은 공개 근거로 다시 닫는다. 내부 설명을 암기하는 대신 어떤 SDK 표면과 cookbook 파일로 같은 주장을 확인할 수 있는지 남긴다.

## 공개판 본문

원래 책은 이 장을 내부 구현과 강의 화면의 대응 관계로 설명한다. 공개판에서는 같은 내용을 "무엇을 설정했는가", "무엇이 메시지로 관측됐는가", "어떤 파일과 notebook으로 재현 가능한가"라는 세 질문으로 바꾼다.

첫째, 설정된 것은 실행 전 계약이다. Agent SDK에서는 model, system prompt, working directory, allowed/disallowed tools, MCP servers, skills/plugins, permission mode 같은 값이 agent가 볼 수 있는 세계를 정한다. 이 값은 말로 설명하는 정책이 아니라 실제 Python 코드와 notebook cell에서 확인되어야 한다.

둘째, 관측된 것은 message stream과 artifact다. assistant text, tool use, tool result, result message, usage/cost, audit log, generated file은 모두 나중에 검증 가능한 증거다. 그래서 이 책의 공개판은 "모델이 그렇게 했을 것이다"라고 쓰지 않고, 어떤 cookbook 파일에서 어떤 실행 표면을 볼 수 있는지 연결한다.

셋째, 추론한 것은 반드시 경계와 함께 둔다. 내부 기능 플래그, 숨은 프롬프트, classifier, cache key, sandbox implementation처럼 공개 SDK나 cookbook으로 확인할 수 없는 항목은 원문을 그대로 게시하지 않는다. 대신 공개 API에서 사용자가 설계할 수 있는 대응 표면으로 바꾸거나, "공개 대응 없음"으로 명시한다.

이 장을 읽을 때는 아래 순서가 좋다.

1. 먼저 주요 cookbook 근거를 열어 실제 notebook이나 Python 파일을 확인한다.
2. `ClaudeAgentOptions`, `query()`, `ClaudeSDKClient`, tool list, MCP config, hooks, session 관련 코드가 어디 있는지 찾는다.
3. 원문 절 제목을 따라가며 내부 설명을 공개 SDK 표면으로 바꿔 적는다.
4. 마지막으로 실습 방향에 맞춰 같은 task를 실행하고 message stream 또는 artifact를 남긴다.

## 공개 경계

- 민감한 prompt/tool data는 telemetry에 그대로 남기지 않는다.
- trace와 audit log는 retention policy가 필요하다.

## 실습 방향

- agent run마다 prompt id, tool calls, errors, cost, final artifact link를 남기는 schema를 만든다.

## Builder takeaway

이 장의 공개판 목표는 원문을 얕게 요약하는 것이 아니다. 책의 논지를 유지하되, 독자가 직접 열어볼 수 있는 Python cookbook과 공식 문서에 묶어 두는 것이다. 따라서 장을 읽은 뒤에는 적어도 하나의 notebook 또는 Python 파일에서 같은 개념을 확인할 수 있어야 한다. 확인할 수 없는 내부 세부는 주장으로 남기지 않고, 공개 경계나 추론으로 분리한다.

## 부록: 이 장을 실제 SDK 실행으로 확인한 결과

> 위 공개판 본문은 이 저장소의 notebook과 공식 문서에 근거해 다시 쓴 것이다. 아래는 같은
> 장의 주장을 실제 Claude Agent SDK로 실행해 관찰한 기록이며, **실측 결과로 위 본문을 고쳐
> 쓰지 않았다.** 본문 설명과 실제 동작이 어긋나는 곳도 본문을 남기고 아래에 근거와 함께 적는다.

**실행 조건** — 실제 모델 `claude-opus-5`, Claude Agent SDK `0.2.128`, Claude Code CLI
`2.1.220`, 시도 `152259-0e968386`. 합성 표식을 심은 실제 MCP 왕복 한 건을 수집한 뒤,
원본에서 OTel까지의 투영과 가림 처리를 감사했다. 원본 사건 36건, 공개 정리 67건이다.

### 본문을 이렇게 읽으면 안 되는 곳

이 장은 **용어를 정확히 쓰는 것 자체가 내용**인 장이다. 실측 결과 다음 이름들이 사실과
어긋났다.

- **"원본(raw) 보관소"는 provider 바이트를 무가공 보존한 파일이 아니다.** 공개 SDK 모양과
  순서는 유지하지만 **저장 전에 자격증명을 제거한 내구성 있는 가림 처리 봉투**라고 불러야 한다.
- **이 OTel은 네이티브 Claude 런타임 OTel이 아니다.** 이 저장소의 내보내기 도구가 기록기 사건을
  투영한 **애플리케이션 계측**이다. 런타임 텔레메트리 환경 변수와 OTLP 내보내기를 사용한 실행의
  증거로 이름 바꾸면 안 된다.
- **원본 수와 span 수가 같다는 것만 완전성 조건으로 쓰면 안 된다.** 그렇게 하면 한 사건을 여러
  관점으로 정당하게 투영한 경우(사건 19)를 실패로 오판한다. **각 원본 사건에 대응 span이 있는지와
  투영 배수 규칙을 함께** 검사해야 한다.
- **모든 원본 사건에 대응 span이 있다는 사실은 "누락이 없다"는 조건일 뿐**, 각 span이 충분한
  의미를 담았다거나 provider 동작 전체를 포착했다는 증명이 아니다.
- **OTel과 원본의 순서가 맞는 것은 독립 관측 두 개가 일치한 것이 아니다.** 같은 기록기가 가림
  처리된 원본을 받은 직후 만든 투영이다.
- **디스크 동기화(`fsync`)를 런타임 사건으로 관찰했다고 쓰면 안 된다.** 구현이 그 호출을 하고
  최종 산출물이 남은 것은 정상 경로의 증거지만, 이번 기록에 시스템 호출 영수증은 없다.
- **강제 플러시와 종료가 호출되고 span 파일이 완성된 것이, 내보내기 실패·디스크 가득·프로세스
  강제 종료 뒤의 재시도나 복구를 증명하지는 않는다.**
- **실제 응답 모델은 어시스턴트 메시지(사건 9·18·29)의 모델 값으로 판정해야 한다.** 과거 코드는
  시작 기록의 모델로 계산했다.
- **"세 어시스턴트 메시지가 Opus 5"를 "실행 전체가 Opus만 썼다"로 확대하면 안 된다.** 종료
  기록에 보조 Haiku가 있다.
- 당시 기록의 소스 해시는 장 원문 바이트만 묶었고 probe·런타임·감사·가림 처리 구현의 해시를
  보존하지 않았다. **현재 코드를 당시 실행의 원인으로 소급 확정하면 안 된다.**

### 이번 실행으로는 확인하지 못한 것

- 네이티브 런타임 텔레메트리 경로의 실제 실행
- 내보내기 실패·디스크 가득·프로세스 종료 뒤의 재시도와 복구
- 시스템 호출 수준의 지속성 영수증

## Codex 최종 검토 의견

이 장의 관찰팩은 해시나 OTel만으로 주장을 참이라고 선언하려는 장치가 아닙니다. 직접 실행 코드가 행동 증거를 만들고, 같은 실행에서 수집된 OTel이 사건 순서와 관계를 보존하며, Speaky가 두 층을 독자가 검토할 수 있는 장면으로 투영합니다. 관찰하지 못한 항목은 이 결합으로도 증명된 것이 아닙니다.

SDK 사건을 보존하고 OTel·교육 그래프·평가로 투영하는 구조는 이 책을 Speaky로 증명하는 핵심이지만 용어를 엄격히 써야 합니다. 실제 Python 코드와 host side effect가 행동의 증거이고, 애플리케이션 OTel은 그 실행 순서의 증거이며, Speaky는 둘을 독자가 검토할 수 있게 합칩니다. 저장 파일은 provider raw bytes가 아니라 redacted SDK envelope이고 같은 recorder의 raw·OTel 일치는 독립 관측이 아니라 무손실 projection 확인입니다. 예제는 최소 SDK 호출, raw message 출력, OTel span, Speaky 링크의 네 단계를 먼저 보여 주고 실패 복구는 별도 probe로 분리해야 합니다. [에이전트 호스팅 노트북](https://nfbs2000.github.io/speaky-claude-cookbooks/notebooks/claude_agent_sdk/07_Hosting_the_agent_kr.html)이 안정된 wire format·세션 저장·OTel 환경의 기준이며, 통합 결과는 [Speaky Agent Flow 29장 재생](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch29)에서 확인할 수 있습니다.
