# 13장: 캐시 아키텍처와 브레이크포인트 설계

이 페이지는 한국어 SDK 책의 `13장: 캐시 아키텍처와 브레이크포인트 설계`을 공개 Python cookbook과 공식 Agent SDK 문서에 맞춰 다시 쓴 공개판이다. 원래 장의 문제의식은 유지하되, 내부 TypeScript 구현명이나 비공개 기능을 그대로 옮기지 않는다. 대신 실행 가능한 notebook, Python 파일, README, 공식 문서를 근거로 사용한다.

**분류:** 제4부: 프롬프트 캐싱<br>
**공개 상태:** `public`<br>
**근거 신뢰도:** `high`<br>
**원문 위치:** `docs/book-sdk-ko/src/part4/ch13.md`

## 이 장의 공개판 요지

이 장은 공개 SDK와 cookbook 예제로 대부분 직접 설명할 수 있다.

공개판에서는 cache breakpoint를 prompt caching cookbook과 Claude Code prompt caching 문서로 설명한다. 긴 시스템/문서 context를 안정적으로 유지하고, 자주 바뀌는 사용자 query나 volatile data를 뒤쪽에 두는 설계를 중심으로 다룬다. Agent SDK에서는 cost tracking과 usage metadata를 함께 보면서 캐시가 실제 비용에 어떤 영향을 주는지 확인한다.

[Prompt caching through the Claude API](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/misc/prompt_caching.ipynb)를 근거로 삼는다. [Speculative prompt caching](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/misc/speculative_prompt_caching.ipynb)를 근거로 삼는다.

!!! evidence "주요 cookbook 근거"
    - [Prompt caching through the Claude API](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/misc/prompt_caching.ipynb)
    - [Speculative prompt caching](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/misc/speculative_prompt_caching.ipynb)
    - [Contextual retrieval with caching](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/capabilities/contextual-embeddings/guide.ipynb)

!!! evidence "공식 문서 근거"
    - [How Claude Code uses prompt caching](https://code.claude.com/docs/en/prompt-caching.md)
    - [Track cost and usage](https://code.claude.com/docs/en/agent-sdk/cost-tracking.md)

## 원문 절 구조를 공개 SDK로 다시 읽기

### 13.1 핵심 질문

이 절은 `캐시 아키텍처와 브레이크포인트 설계`의 질문을 공개 SDK에서 관측 가능한 사건으로 좁힌다. 답은 내부 구현명이 아니라 `ClaudeAgentOptions`, message stream, tool call, session record, cookbook 실행 결과에서 찾아야 한다.

### 13.2 캐시는 prefix 안정성 게임이다

이 절은 prompt caching의 prefix 안정성, 입력 변화, cost/latency 관측 문제로 읽는다. 내부 cache key를 추정하지 않고 cookbook의 caching notebook과 usage 데이터를 근거로 삼는다.

### 13.3 SDK에서 설계할 수 있는 캐시 경계

이 절은 prompt caching의 prefix 안정성, 입력 변화, cost/latency 관측 문제로 읽는다. 내부 cache key를 추정하지 않고 cookbook의 caching notebook과 usage 데이터를 근거로 삼는다.

### 13.4 global/org/cache 없음의 강의 번역

이 절은 원문의 `13.4 global/org/cache 없음의 강의 번역` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 공개판에서는 cache breakpoint를 prompt caching cookbook과 Claude Code prompt caching 문서로 설명한다. 긴 시스템/문서 context를 안정적으로 유지하고, 자주 바뀌는 사용자 query나 volatile data를 뒤쪽에 두는 설계를 중심으로 다룬다.... 이며, 먼저 `Prompt caching through the Claude API`를 기준 예제로 읽는다.

### 13.5 MCP와 스킬은 캐시의 적이 될 수 있다

이 절은 prompt caching의 prefix 안정성, 입력 변화, cost/latency 관측 문제로 읽는다. 내부 cache key를 추정하지 않고 cookbook의 caching notebook과 usage 데이터를 근거로 삼는다.

### 13.6 캔버스 표현

이 절은 화면 구성의 문제가 아니라 evidence projection 문제로 읽는다. prompt, tool use, tool result, final result, usage를 한 화면에서 분리해 보여주는 구조가 필요하다.

### 13.7 학생 실습

이 절은 강의용 실습으로 바꾼다. 실습은 관련 notebook을 실행하고, 입력 옵션과 tool call, 중간 결과, 최종 artifact를 함께 기록하는 방식이어야 한다.

### Takeaway

이 절의 결론은 공개 근거로 다시 닫는다. 내부 설명을 암기하는 대신 어떤 SDK 표면과 cookbook 파일로 같은 주장을 확인할 수 있는지 남긴다.

## 공개판 본문

원래 책은 이 장을 내부 구현과 강의 화면의 대응 관계로 설명한다. 공개판에서는 같은 내용을 "무엇을 설정했는가", "무엇이 메시지로 관측됐는가", "어떤 파일과 notebook으로 재현 가능한가"라는 세 질문으로 바꾼다.

첫째, 설정된 것은 실행 전 계약이다. Agent SDK에서는 model, system prompt, working directory, allowed/disallowed tools, MCP servers, skills/plugins, permission mode 같은 값이 agent가 볼 수 있는 세계를 정한다. 이 값은 말로 설명하는 정책이 아니라 실제 Python 코드와 notebook cell에서 확인되어야 한다.

둘째, 관측된 것은 message stream과 artifact다. assistant text, tool use, tool result, result message, usage/cost, audit log, generated file은 모두 나중에 검증 가능한 증거다. 그래서 이 책의 공개판은 "모델이 그렇게 했을 것이다"라고 쓰지 않고, 어떤 cookbook 파일에서 어떤 실행 표면을 볼 수 있는지 연결한다.

셋째, 추론한 것은 반드시 경계와 함께 둔다. 내부 기능 플래그, 숨은 프롬프트, classifier, cache key, sandbox implementation처럼 공개 SDK나 cookbook으로 확인할 수 없는 항목은 원문을 그대로 게시하지 않는다. 대신 공개 API에서 사용자가 설계할 수 있는 대응 표면으로 바꾸거나, "공개 대응 없음"으로 명시한다.

이 장을 읽을 때는 아래 순서가 좋다.

1. 먼저 주요 cookbook 근거를 열어 실제 notebook이나 Python 파일을 확인한다.
2. `ClaudeAgentOptions`, `query()`, `ClaudeSDKClient`, tool list, MCP config, hooks, session 관련 코드가 어디 있는지 찾는다.
3. 원문 절 제목을 따라가며 내부 설명을 공개 SDK 표면으로 바꿔 적는다.
4. 마지막으로 실습 방향에 맞춰 같은 task를 실행하고 message stream 또는 artifact를 남긴다.

## 공개 경계

- 내부 cache key 구성은 공개하지 않는다.
- 브레이크포인트는 prompt layout과 usage 결과로만 설명한다.

## 실습 방향

- 고정 context와 변경 context를 분리해 cache hit 변화와 latency를 측정한다.

## Builder takeaway

이 장의 공개판 목표는 원문을 얕게 요약하는 것이 아니다. 책의 논지를 유지하되, 독자가 직접 열어볼 수 있는 Python cookbook과 공식 문서에 묶어 두는 것이다. 따라서 장을 읽은 뒤에는 적어도 하나의 notebook 또는 Python 파일에서 같은 개념을 확인할 수 있어야 한다. 확인할 수 없는 내부 세부는 주장으로 남기지 않고, 공개 경계나 추론으로 분리한다.

## 부록: 이 장을 실제 SDK 실행으로 확인한 결과

> 위 공개판 본문은 이 저장소의 notebook과 공식 문서에 근거해 다시 쓴 것이다. 아래는 같은
> 장의 주장을 실제 Claude Agent SDK로 실행해 관찰한 기록이며, **실측 결과로 위 본문을 고쳐
> 쓰지 않았다.** 본문 설명과 실제 동작이 어긋나는 곳도 본문을 남기고 아래에 근거와 함께 적는다.

**실행 조건** — 실제 모델 `claude-opus-5`, Claude Agent SDK `0.2.128`, 시도 `232127-6b9cb9b3`.
7,806자 고정 앞부분에 서로 다른 뒷부분을 붙여 **독립된 두 세션**으로 순차 실행했다
(A 먼저 끝낸 뒤 B 시작). 원본 사건 36건, 공개 정리 38건이다.

### 실제로 확인된 것

- 두 실행은 서로 다른 session ID를 사용했고(시작 기록 2와 22), A 종료(20) 다음에 B 시작(21)이
  와서 **병렬 실행이 아님**이 기록으로 확인된다.
- **A** — 캐시 생성 2,570, 캐시 읽기 0, 표식 A, 성공 종료.
- **B** — 캐시 생성 143, 캐시 읽기 2,427, 표식 B, 성공 종료.
- 서로 다른 뒷부분을 받은 두 실행이 각각 정확한 표식을 돌려주었고 둘 다 성공했다.
- 두 시작 기록의 도구 목록이 모두 비어 있어, 이 쌍에는 모델이 볼 도구 표면이 없었다.
- 원본 SDK 30건과 OTel 30건의 순서까지 일치하고 38개 span이 한 trace에 속한다.
- 무결성 해시 7개, 그리고 실행 당시 장·probe·case·런타임 소스 해시 4개가 모두 일치했다.

### 관찰값에서 추론한 것 (관찰이 아니다)

B의 읽기 2,427과 생성 143의 합이 A의 생성 2,570과 같고, 호스트가 바꾼 것은 뒷부분뿐이다.
이는 **긴 앞부분의 대부분이 재사용됐음을 강하게 지지한다.** 다만 서버가 어느 구간을 어떻게
나눴는지 알려 주는 정보는 없으므로, 이 귀속은 관찰이 아니라 추론이다.

### 본문을 이렇게 읽으면 안 되는 곳

- **Messages API 예제의 `cache_control`을 설치된 `ClaudeAgentOptions`의 필드처럼 설명하면 안
  된다.** 이 실험은 시스템 프롬프트와 종료 사용량 표면을 사용했다.
- **호스트가 기록한 시스템 프롬프트 해시는 통제 입력의 증거일 뿐, 서버 캐시 키 구성의 증거가
  아니다.**
- 주 모델은 Opus 5였지만 종료 기록에 Haiku 4.5도 함께 있었다.
- OTel의 `CAPTURED`·단정 수 0·판정 `TODO`는 수집 완료와 수동 판독 대기 상태다. 같은
  기록기가 투영했으므로 독립 관측기가 아니다.

### 이번 실행에서 관찰되지 않은 것

- 전역 또는 조직 단위 캐시 범위 정보는 원본과 종료 사용량 어디에도 없다.
- 사용량에 1시간 관련 필드 이름은 보였지만, 시간 경계를 넘겨 호출하지 않았으므로 **실제 만료
  시점은 관찰하지 못했다.**
- 한 쌍의 A/B는 반복 분포가 아니므로 **캐시가 지연을 개선했다는 인과는 관찰하지 못했다.**
- 도구와 MCP 표면이 비어 있었으므로, 스키마 변경이 캐시를 깨뜨리는지는 이 실행에서 볼 수
  없었다.

### 앞으로 필요한 관측

- 5분·1시간 전후로 같은 앞부분을 순차 호출해 실제 만료 경계 측정
- 같은 조건 반복 표본으로 캐시 사용량과 지연·비용 분포 분리
- 도구나 MCP 스키마 하나만 바꾼 A/B로 캐시 읽기 구간 변화 측정
- 문자열 시스템 프롬프트와 preset append/동적 구역 제외의 캐시 차이
