# 2장: 도구 시스템 - 모델의 손이 되는 40개 이상의 도구

이 페이지는 한국어 SDK 책의 `2장: 도구 시스템 - 모델의 손이 되는 40개 이상의 도구`을 공개 Python cookbook과 공식 Agent SDK 문서에 맞춰 다시 쓴 공개판이다. 원래 장의 문제의식은 유지하되, 내부 TypeScript 구현명이나 비공개 기능을 그대로 옮기지 않는다. 대신 실행 가능한 notebook, Python 파일, README, 공식 문서를 근거로 사용한다.

**분류:** 제1부: 아키텍처<br>
**공개 상태:** `public-rewrite`<br>
**근거 신뢰도:** `high`<br>
**원문 위치:** `docs/book-sdk-ko/src/part1/ch02.md`

## 이 장의 공개판 요지

이 장은 원문의 내부 구현 표현을 공개 SDK 옵션, 메시지, 도구, 세션, notebook 실행 증거로 바꿔 읽어야 한다.

이 장은 공개판에서 built-in tools, custom tools, MCP tools, tool search의 네 갈래로 나눈다. 공개 SDK 문서는 Read, Write, Edit, Bash, Glob, Grep, WebSearch 같은 built-in tools와 MCP/custom tools를 설명한다. cookbook은 tool choice, Pydantic tool schema, parallel tools, tool search embeddings로 도구 표면을 실습한다. 원문의 도구 등록 파이프라인은 공개 SDK의 `allowed_tools`, `disallowed_tools`, `mcp_servers`, custom tools, permission evaluation으로 바꿔 설명한다. 많은 도구 문제는 tool search cookbook과 공식 tool search 문서로 연결한다.

[Tool choice notebook](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/tool_choice.ipynb)를 근거로 삼는다. [Tool use with Pydantic](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/tool_use_with_pydantic.ipynb)를 근거로 삼는다.

!!! evidence "주요 cookbook 근거"
    - [Tool choice notebook](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/tool_choice.ipynb)
    - [Tool use with Pydantic](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/tool_use_with_pydantic.ipynb)
    - [Tool search with embeddings](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/tool_search_with_embeddings.ipynb)
    - [Parallel tools notebook](https://github.com/nfbs2000/speaky-claude-cookbooks/blob/main/tool_use/parallel_tools.ipynb)

!!! evidence "공식 문서 근거"
    - [Give Claude custom tools](https://code.claude.com/docs/en/agent-sdk/custom-tools.md)
    - [Scale to many tools with tool search](https://code.claude.com/docs/en/agent-sdk/tool-search.md)
    - [Tools reference](https://code.claude.com/docs/en/tools-reference.md)

## 원문 절 구조를 공개 SDK로 다시 읽기

### 핵심 질문

이 절은 `도구 시스템 - 모델의 손이 되는 40개 이상의 도구`의 질문을 공개 SDK에서 관측 가능한 사건으로 좁힌다. 답은 내부 구현명이 아니라 `ClaudeAgentOptions`, message stream, tool call, session record, cookbook 실행 결과에서 찾아야 한다.

### 2.1 공개 TypeScript 타입으로 읽기

이 절은 원문의 `2.1 공개 TypeScript 타입으로 읽기` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 이 장은 공개판에서 built-in tools, custom tools, MCP tools, tool search의 네 갈래로 나눈다. 공개 SDK 문서는 Read, Write, Edit, Bash, Glob, Grep, WebSearch 같은 built-in tools와 MCP/custom tools를... 이며, 먼저 `Tool choice notebook`를 기준 예제로 읽는다.

### 2.2 공식 권한 평가 순서와 안전 기본값

이 절은 `allowed_tools`, `disallowed_tools`, `permission_mode`, user approval, hooks의 공개 권한 표면으로 재작성한다.

### 2.3 SDK에서 도구 표면을 구성하는 방법

이 절은 built-in tools, custom tools, MCP tools, tool result schema의 문제로 재작성한다. 도구는 모델의 능력이 아니라 외부 세계와 만나는 계약이다.

### 2.4 built-in tools와 custom tools

이 절은 원문의 `2.4 built-in tools와 custom tools` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 이 장은 공개판에서 built-in tools, custom tools, MCP tools, tool search의 네 갈래로 나눈다. 공개 SDK 문서는 Read, Write, Edit, Bash, Glob, Grep, WebSearch 같은 built-in tools와 MCP/custom tools를... 이며, 먼저 `Tool choice notebook`를 기준 예제로 읽는다.

### 2.5 도구 등록 파이프라인을 SDK 화면으로 보여주기

이 절은 built-in tools, custom tools, MCP tools, tool result schema의 문제로 재작성한다. 도구는 모델의 능력이 아니라 외부 세계와 만나는 계약이다.

### 2.6 결과 크기 예산과 persisted evidence

이 절은 원문의 `2.6 결과 크기 예산과 persisted evidence` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 이 장은 공개판에서 built-in tools, custom tools, MCP tools, tool search의 네 갈래로 나눈다. 공개 SDK 문서는 Read, Write, Edit, Bash, Glob, Grep, WebSearch 같은 built-in tools와 MCP/custom tools를... 이며, 먼저 `Tool choice notebook`를 기준 예제로 읽는다.

### 2.7 3단계 렌더링을 SDK UI로 번역하기

이 절은 원문의 `2.7 3단계 렌더링을 SDK UI로 번역하기` 논지를 공개 Python cookbook의 실행 가능한 근거로 옮긴다. 핵심은 이 장은 공개판에서 built-in tools, custom tools, MCP tools, tool search의 네 갈래로 나눈다. 공개 SDK 문서는 Read, Write, Edit, Bash, Glob, Grep, WebSearch 같은 built-in tools와 MCP/custom tools를... 이며, 먼저 `Tool choice notebook`를 기준 예제로 읽는다.

### 2.8 ToolSearch와 많은 도구 문제

이 절은 built-in tools, custom tools, MCP tools, tool result schema의 문제로 재작성한다. 도구는 모델의 능력이 아니라 외부 세계와 만나는 계약이다.

### 2.9 학생 실습

이 절은 강의용 실습으로 바꾼다. 실습은 관련 notebook을 실행하고, 입력 옵션과 tool call, 중간 결과, 최종 artifact를 함께 기록하는 방식이어야 한다.

### Builder takeaway

이 절의 결론은 builder checklist로 바꾼다. agent를 만들 때 prompt, tools, permissions, memory, observability, deployment 중 어느 경계를 설계해야 하는지 정리한다.

## 공개판 본문

원래 책은 이 장을 내부 구현과 강의 화면의 대응 관계로 설명한다. 공개판에서는 같은 내용을 "무엇을 설정했는가", "무엇이 메시지로 관측됐는가", "어떤 파일과 notebook으로 재현 가능한가"라는 세 질문으로 바꾼다.

첫째, 설정된 것은 실행 전 계약이다. Agent SDK에서는 model, system prompt, working directory, allowed/disallowed tools, MCP servers, skills/plugins, permission mode 같은 값이 agent가 볼 수 있는 세계를 정한다. 이 값은 말로 설명하는 정책이 아니라 실제 Python 코드와 notebook cell에서 확인되어야 한다.

둘째, 관측된 것은 message stream과 artifact다. assistant text, tool use, tool result, result message, usage/cost, audit log, generated file은 모두 나중에 검증 가능한 증거다. 그래서 이 책의 공개판은 "모델이 그렇게 했을 것이다"라고 쓰지 않고, 어떤 cookbook 파일에서 어떤 실행 표면을 볼 수 있는지 연결한다.

셋째, 추론한 것은 반드시 경계와 함께 둔다. 내부 기능 플래그, 숨은 프롬프트, classifier, cache key, sandbox implementation처럼 공개 SDK나 cookbook으로 확인할 수 없는 항목은 원문을 그대로 게시하지 않는다. 대신 공개 API에서 사용자가 설계할 수 있는 대응 표면으로 바꾸거나, "공개 대응 없음"으로 명시한다.

이 장을 읽을 때는 아래 순서가 좋다.

1. 먼저 주요 cookbook 근거를 열어 실제 notebook이나 Python 파일을 확인한다.
2. `ClaudeAgentOptions`, `query()`, `ClaudeSDKClient`, tool list, MCP config, hooks, session 관련 코드가 어디 있는지 찾는다.
3. 원문 절 제목을 따라가며 내부 설명을 공개 SDK 표면으로 바꿔 적는다.
4. 마지막으로 실습 방향에 맞춰 같은 task를 실행하고 message stream 또는 artifact를 남긴다.

## 공개 경계

- 비공개 도구 전체 목록처럼 보이는 내용은 게시하지 않는다.
- 도구별 내부 prompt 원문은 공개 tool description 설계 원칙으로 바꾼다.

## 실습 방향

- `tool_choice.ipynb`에서 tool forcing과 자유 선택 결과를 비교한다.
- `tool_search_with_embeddings.ipynb`를 기준으로 많은 도구를 한 번에 노출하지 않는 설계를 설명한다.

## Builder takeaway

이 장의 공개판 목표는 원문을 얕게 요약하는 것이 아니다. 책의 논지를 유지하되, 독자가 직접 열어볼 수 있는 Python cookbook과 공식 문서에 묶어 두는 것이다. 따라서 장을 읽은 뒤에는 적어도 하나의 notebook 또는 Python 파일에서 같은 개념을 확인할 수 있어야 한다. 확인할 수 없는 내부 세부는 주장으로 남기지 않고, 공개 경계나 추론으로 분리한다.

## 부록: 이 장을 실제 SDK 실행으로 확인한 결과

> 위 공개판 본문은 이 저장소의 notebook과 공식 문서에 근거해 다시 쓴 것이다. 아래는 같은
> 장의 주장을 실제 Claude Agent SDK로 실행해 관찰한 기록이며, **실측 결과로 위 본문을 고쳐
> 쓰지 않았다.** 본문 설명과 실제 동작이 어긋나는 곳도 본문을 남기고 아래에 근거와 함께 적는다.

**실행 조건** — 실제 모델 `claude-opus-5`, Claude Agent SDK `0.2.128`. 권한 평가 순서를 보기
위해 다섯 가지 조건을 하나씩 따로 실행했다(`044555-3c0f4677`, `044648-35a833a2`,
`044804-d7f879b9`, `044856-5d71aa95`, `044935-0d97290e`). 원본 사건 208건 중 54건을 정리했다.

### 실제로 확인된 것

- **승인되지 않은 도구는 콜백까지 올라온다.** 미승인 상태의 custom MCP 요청은
  `can_use_tool`에 권한 요청으로 도달했고, 콜백이 allow를 돌려준 뒤에야 실제 handler가 돌고
  도구 결과가 나왔다.
- **미리 허용해 두면 콜백을 아예 지나간다.** 같은 도구를 `allowed_tools`에 넣자
  `can_use_tool`은 한 번도 호출되지 않고 handler가 곧바로 실행됐다. 즉 콜백은 "모든 도구
  실행을 지켜보는 감시 지점"이 아니다.
- **훅의 거부가 허용 규칙보다 앞섭니다.** `allowed_tools`에 든 도구라도 `PreToolUse`가 deny를
  반환하면 handler는 실행되지 않고 오류 도구 결과로 닫혔다.
- **`dontAsk`는 질문 없이 거절한다.** 앱 콜백을 아예 두지 않은 상태에서 미승인 도구를
  요청하니 오류 도구 결과와 권한 거절 기록으로 끝났다.

### 본문을 이렇게 읽으면 안 되는 곳

- **시작 시점의 도구 목록(`init.tools`)을 "실행 허가 목록"으로 읽으면 안 된다.** 명시적으로
  거부한 custom MCP 도구의 *이름은 목록에 그대로 남아 있었고*, 모델은 그 도구를 호출하기까지
  했다. 그런데도 실행 단계에서 차단됐다. `bypassPermissions`에서도 마찬가지였다.
  목록에 이름이 보인다는 것은 "모델이 부를 수 있다"는 뜻이고, 실제로 실행되는지는 권한 평가가
  따로 결정한다.
  (근거: `044804-d7f879b9` SDK 메시지 1·16·17·19·43)

### 이번 실행으로는 확인하지 못한 것

- built-in 도구를 `disallowed_tools`로 막았을 때 도구 목록에서 이름 자체가 사라지는지 — 위
  결과는 custom MCP 도구로 얻은 것이라 그대로 일반화하지 않았다.
- 큰 도구 결과의 저장 경로, 도구 진행 상황 메시지, ToolSearch, 캐시 효과

## Codex 최종 검토 의견

이 장의 관찰팩은 해시나 OTel만으로 주장을 참이라고 선언하려는 장치가 아닙니다. 직접 실행 코드가 행동 증거를 만들고, 같은 실행에서 수집된 OTel이 사건 순서와 관계를 보존하며, Speaky가 두 층을 독자가 검토할 수 있는 장면으로 투영합니다. 관찰하지 못한 항목은 이 결합으로도 증명된 것이 아닙니다.

도구를 `tool_use`와 `tool_result`의 왕복으로 설명한 핵심은 정확하지만, 권한은 도구 노출과 자동 승인을 분리해 읽어야 합니다. `allowedTools`를 노출 제한으로 해석하지 말고 hook, deny, ask, permission mode, allow, `canUseTool`, 실제 handler 결과를 순서대로 관찰해야 합니다. 관찰팩은 이 순서를 직접 실행한 Python callback과 SDK 메시지로 확인하고, Speaky는 같은 호출 ID가 승인과 실행으로 이어졌는지를 보여 주기 때문에 붙습니다. 예제는 복잡한 recorder보다 `auto`·특정 도구 강제·거부를 각각 한 변수로 실행하는 세 사례로 줄이면 이해가 빠릅니다. [도구 선택 노트북](https://nfbs2000.github.io/speaky-claude-cookbooks/notebooks/tool_use/tool_choice_kr.html)에서 기본 도구 선택 코드를, [Speaky Agent Flow 2장 재생](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch02)에서 권한 사건의 실제 순서를 확인할 수 있습니다.
