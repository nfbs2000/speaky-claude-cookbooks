<!-- Generated by scripts/publish_book_sdk_live_evidence.mjs. -->

# 19장 실제 Python SDK 관찰

이 페이지는 cookbook 예제를 설명하는 정적 해설이 아니라, Python Claude Agent SDK를 실제
`claude-opus-5`로 순차 호출해 저장한 공개 증거다. 테스트 assertion은 recorder와
projection 코드의 계약만 검증하며, 아래 판정은 raw SDK message, permission callback, host process
event와 artifact snapshot 중 이 장이 실제로 발생시킨 사건에서 나왔다.

!!! evidence "한 번 수집하고 여러 표면에서 재생"
    원전은 46개 사건이고 공개 projection은 62개다.
    credential, private prompt, 비공개 추론 payload, 사용자 절대 경로는 공개하지 않았다. 공개 사건은
    source event ID와 SHA-256을 유지한다.

## 실행 신원

| 항목 | 값 |
| --- | --- |
| campaign | `book-sdk-live-20260804` |
| replay model | `claude-opus-5` |
| supporting 포함 cited model | `claude-opus-5` |
| proof gate | `accepted_with_notes` |
| secret scan | `passed` |
| raw source / public | **46 / 62** |
| configured / observed / inferred | **3 / 13 / 1** |
| not observed / correction / more evidence | **9 / 11 / 5** |
| trace SHA-256 | `6407b853e713a35ee4abe5749a5165637b82f804b1efd47301f08a76d50a2f7a` |
| summary SHA-256 | `ad82be14aa62a7c96729b6eefd515ddad5dd6465c2dfad009133b2cde99a7134` |

## 무결성 검증된 실제 실행과 보조 출처

| attempt | source campaign | source chapter | role | case | actual model | source events | raw SDK SHA-256 | host process SHA-256 |
| --- | --- | --- | --- | --- | --- | ---: | --- | --- |
| `061423-c0aa2799` | `book-sdk-live-20260804` | `ch19` | **replayed** | `setting-sources-empty` | `claude-opus-5` | 17 | `ce4f5f632ac3dc41535235b3cc00e5269c1e5544074aa6b53c95d06ccf2331a1` | `864f27d68e213bbb6d9c02f76152b2e13e4644a507826d004d1f172fb151e147` |
| `061441-31bd87d5` | `book-sdk-live-20260804` | `ch19` | **replayed** | `setting-sources-project` | `claude-opus-5` | 29 | `e88ac9f1217d55bf4eb97b658463850b7f84f51e4c25f769978d86ccb413e52d` | `cfc26fc769eff39f0e1731012715a4e47ffd8c21026e381be81bcb1bdeff62d5` |

`supporting-only`는 다른 장 또는 이전 campaign의 원본 실행을 이 장의 claim 출처로만 인용한다는 뜻이다.
그 실행은 이 장의 replay timeline에 다시 삽입하거나 복제하지 않는다.

## 관찰 판정

| claim | 판정 | 실제 관찰 또는 경계 | 첫 source ref |
| --- | --- | --- | --- |
| `setting-source-surface-configured` | **설정됨** | Python Agent SDK 0.2.128의 SettingSource는 user, project, local이고 ClaudeAgentOptions.setting_sources는 None, 빈 list, 선택 source list를 구분한다. 문서상 None은 CLI 기본 source, []는 filesystem setting 비활성, project는 CLAUDE.md 로드 조건이다. | `spec:claims:setting-source-surface-configured` |
| `controlled-pair-configured` | **설정됨** | 두 attempt는 같은 chapter bytes, probe source hashes, Opus 5, Read 단일 tool, 동일 CLAUDE.md와 evidence marker, provider concurrency 1을 사용하고 setting_sources만 []와 [project]로 나눴다. | `attempt:061423-c0aa2799:manifest:option_summary` |
| `safe-readonly-prompt-configured` | **설정됨** | 프롬프트는 disposable temporary workspace의 읽기 전용 marker 과업으로 제한했고, loaded instruction이 명시하지 않은 경로 탐색과 관련 없는 작업을 금지했다. 보안 우회나 권한 상승은 요청하지 않았다. | `spec:claims:safe-readonly-prompt-configured` |
| `assistant-messages-used-opus5` | **관찰됨** | empty attempt의 AssistantMessage 1건과 project attempt의 AssistantMessage 2건은 모두 claude-opus-5였다. actual model은 requested 값이나 init 값이 아니라 AssistantMessage.model로 판정했다. | `attempt:061423-c0aa2799:sdk-message:10` |
| `instruction-files-identical` | **관찰됨** | 두 attempt의 host record에는 같은 306자 CLAUDE.md와 27자 evidence file이 있었고 각각의 SHA-256도 동일했다. | `attempt:061423-c0aa2799:process-event:1` |
| `empty-memory-files-remained-empty` | **관찰됨** | setting_sources=[] attempt의 get_context_usage() memoryFiles는 query 전 sequence 2와 응답 후 sequence 15 모두 빈 배열이었다. | `attempt:061423-c0aa2799:process-event:2` |
| `project-memory-file-loaded-before-query` | **관찰됨** | setting_sources=[project] attempt는 model query 전 sequence 2부터 memoryFiles에 <workspace>/CLAUDE.md, type Project, 149 tokens를 보고했고 응답 후 sequence 27에도 유지했다. | `attempt:061441-31bd87d5:process-event:2` |
| `empty-source-used-no-tool` | **관찰됨** | setting_sources=[] attempt는 ToolUse와 ToolResult가 모두 0건이었고 AssistantMessage와 terminal Result가 NO_PROJECT_INSTRUCTION을 반환했다. | `attempt:061423-c0aa2799:sdk-message:10` |
| `project-source-read-once` | **관찰됨** | setting_sources=[project] attempt는 sequence 12에서 Read ToolUse를 정확히 한 번 만들었고 대상은 임시 workspace의 docs/evidence.txt였다. | `attempt:061441-31bd87d5:sdk-message:12` |
| `project-tool-result-contained-marker` | **관찰됨** | project attempt의 sequence 14 ToolResult는 CH19_EVIDENCE_FILE_CONTENT를 반환했고 sequence 12 ToolUse ID와 연결됐다. | `attempt:061441-31bd87d5:sdk-message:14` |
| `terminal-results-followed-source-boundary` | **관찰됨** | empty attempt는 sequence 14에서 NO_PROJECT_INSTRUCTION으로, project attempt는 sequence 26에서 PROJECT_INSTRUCTION_APPLIED marker로 각각 success 종료했다. | `attempt:061423-c0aa2799:sdk-message:14` |
| `hook-event-messages-absent` | **관찰됨** | include_hook_events=True였지만 두 attempt 모두 HookEventMessage는 0건이었다. project instruction load를 HookEvent로 주장하지 않는다. | `attempt:061423-c0aa2799:process-event:16` |
| `current-cli-221-observed` | **관찰됨** | 두 새 attempt의 manifest와 init은 Claude Code 2.1.221, Python Agent SDK 0.2.128을 기록했다. | `attempt:061423-c0aa2799:manifest:option_summary` |
| `attempt-integrity-files-match` | **관찰됨** | 두 attempt 모두 integrity manifest가 선언한 manifest, verdict, raw SDK/process/callback, OTel 파일 7개의 SHA-256을 출판기가 다시 계산해 일치했다. | `spec:claims:attempt-integrity-files-match` |
| `raw-and-otel-sequences-align` | **관찰됨** | empty OTel은 19 spans, project OTel은 31 spans였고 각 attempt의 trace ID는 하나였다. sdk.message와 process.event span은 raw recorder sequence와 type을 보존했다. | `spec:claims:raw-and-otel-sequences-align` |
| `auxiliary-haiku-usage-recorded` | **관찰됨** | 두 terminal Result의 model_usage에는 claude-opus-5와 claude-haiku-4-5-20251001 key가 모두 있었다. AssistantMessage 응답 모델과 provider 내부 보조 사용량을 구분해야 한다. | `attempt:061423-c0aa2799:sdk-message:14` |
| `project-source-caused-pair-difference` | **추론됨** | 동일한 임시 파일, tool surface, prompt와 모델에서 의도적으로 바꾼 변수는 setting_sources였고 memoryFiles와 실제 행동이 함께 갈라졌으므로, 이 통제 pair에서는 project source 활성화를 차이의 원인으로 해석한다. 모든 source 조합에 일반화하지 않는다. | `attempt:061423-c0aa2799:process-event:2` |
| `host-file-presence-is-not-load-proof` | **범위 수정 필요** | CLAUDE.md가 두 workspace에 모두 존재했다는 사실만으로 load를 확정하면 안 된다. empty pair는 같은 파일이 있어도 memoryFiles가 비어 있었다. | `attempt:061423-c0aa2799:process-event:1` |
| `model-self-report-is-not-load-proof` | **범위 수정 필요** | 모델이 지침을 읽었다고 서술하는지 여부는 load 증명이 아니다. current SDK에서는 memoryFiles inventory와 실제 tool/result를 우선해야 한다. | `spec:claims:model-self-report-is-not-load-proof` |
| `init-model-is-not-response-model-proof` | **범위 수정 필요** | System init.model은 configured model이다. 보안 거부 뒤 lower-model fallback을 놓치지 않으려면 actual model은 AssistantMessage.model의 단일성으로 판정해야 한다. | `spec:claims:init-model-is-not-response-model-proof` |
| `context-usage-model-is-not-response-model-proof` | **범위 수정 필요** | get_context_usage().model도 context 계산 대상 모델이지 응답 메시지 자체의 모델 증거로 대체하지 않는다. | `spec:claims:context-usage-model-is-not-response-model-proof` |
| `opus-response-is-not-opus-only-provider-run` | **범위 수정 필요** | 모든 AssistantMessage가 Opus 5였다는 관찰을 provider run 전체가 Opus만 사용했다는 말로 확대하면 안 된다. Result model_usage에는 Haiku 보조 사용량도 있었다. | `attempt:061423-c0aa2799:sdk-message:14` |
| `memory-files-is-not-security-boundary` | **범위 수정 필요** | memoryFiles에 CLAUDE.md가 나타나는 것은 context load 증거이지 permission, hook 또는 sandbox 강제를 뜻하지 않는다. CLAUDE.md는 모델 지침이다. | `attempt:061441-31bd87d5:process-event:2` |
| `instructions-loaded-is-not-current-hook` | **범위 수정 필요** | InstructionsLoaded를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다. | `spec:claims:instructions-loaded-is-not-current-hook` |
| `config-change-is-not-current-hook` | **범위 수정 필요** | ConfigChange를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다. | `spec:claims:config-change-is-not-current-hook` |
| `file-changed-is-not-current-hook` | **범위 수정 필요** | FileChanged를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다. | `spec:claims:file-changed-is-not-current-hook` |
| `none-and-empty-setting-sources-differ` | **범위 수정 필요** | setting_sources=None과 []를 같은 것으로 설명하면 안 된다. SDK 0.2.128 문서상 None은 CLI 기본 source를 따르고 []는 filesystem setting을 비활성화한다. | `spec:claims:none-and-empty-setting-sources-differ` |
| `otel-is-not-independent-runtime-proof` | **범위 수정 필요** | OTel은 같은 AttemptRecorder가 raw SDK와 host process event를 투영한 결과다. sequence 정합성은 수집 파이프라인 검증이지 독립 provider 관측이 아니다. | `spec:claims:otel-is-not-independent-runtime-proof` |
| `full-source-precedence-not-observed` | **관찰되지 않음** | managed, user, project, local 전체 충돌 우선순위는 이번 [] 대 [project] pair에서 실행하지 않았다. SettingSource literal 자체도 user, project, local 세 값이다. | `spec:claims:full-source-precedence-not-observed` |
| `include-resolution-not-observed` | **관찰되지 않음** | CLAUDE.md의 @include 해석, 누락 파일, 재귀와 순환 규칙은 실행하지 않았다. | `spec:claims:include-resolution-not-observed` |
| `path-frontmatter-not-observed` | **관찰되지 않음** | frontmatter paths의 일치 또는 불일치 작업 범위는 실행하지 않았다. | `spec:claims:path-frontmatter-not-observed` |
| `html-comment-processing-not-observed` | **관찰되지 않음** | HTML comment 안팎의 instruction 처리 또는 stripping은 실행하지 않았다. | `spec:claims:html-comment-processing-not-observed` |
| `size-budget-not-observed` | **관찰되지 않음** | 40K 또는 다른 instruction size 경계 전후의 sentinel 보존은 실행하지 않았다. | `spec:claims:size-budget-not-observed` |
| `runtime-file-change-not-observed` | **관찰되지 않음** | 동일 session에서 CLAUDE.md를 수정한 전후의 memoryFiles 또는 행동 갱신은 실행하지 않았다. | `spec:claims:runtime-file-change-not-observed` |
| `claude-rules-traversal-not-observed` | **관찰되지 않음** | .claude/rules 순회와 하위 경로별 rule 적용은 실행하지 않았다. | `spec:claims:claude-rules-traversal-not-observed` |
| `default-none-behavior-not-observed` | **관찰되지 않음** | SDK 문서에는 setting_sources=None의 기본 동작이 기술돼 있지만 이번 live pair는 None case를 실행하지 않았다. | `spec:claims:default-none-behavior-not-observed` |
| `skills-source-interaction-not-observed` | **관찰되지 않음** | skills option이 setting_sources 기본값과 tool surface를 보강하는 현재 SDK 경로는 이번 skills=None pair에서 인과 실험하지 않았다. | `spec:claims:skills-source-interaction-not-observed` |
| `precedence-matrix-required` | **추가 관측 필요** | user, project, local과 별도 managed policy의 충돌 우선순위를 말하려면 source별 상반 marker를 격리한 순차 matrix가 필요하다. | `spec:claims:precedence-matrix-required` |
| `instruction-feature-matrix-required` | **추가 관측 필요** | include, path scope, comment 처리, size budget, .claude/rules는 각 기능을 한 변수씩 바꾼 별도 temp fixture가 필요하다. | `spec:claims:instruction-feature-matrix-required` |
| `session-change-tracking-required` | **추가 관측 필요** | 실행 중 변경 추적은 같은 client session에서 query 사이에 파일을 바꾸고 memoryFiles와 marker를 전후 수집해야 한다. | `spec:claims:session-change-tracking-required` |
| `none-and-skills-live-cases-required` | **추가 관측 필요** | setting_sources=None 기본과 skills auto-configuration은 문서 표면과 실제 memoryFiles/tool listing을 분리해 추가 관찰해야 한다. | `spec:claims:none-and-skills-live-cases-required` |
| `context-usage-unavailable-path-required` | **추가 관측 필요** | get_context_usage가 지원되지 않거나 오류를 반환하는 CLI 조합에서 행동 증거로 어떻게 fallback할지는 별도 호환성 case가 필요하다. | `spec:claims:context-usage-unavailable-path-required` |

중요한 판정 규칙은 다음과 같다.

- **관찰됨**: 기대 사건과 순서가 이번 raw evidence에 직접 존재한다.
- **설정됨**: host가 입력과 실행 조건을 고정했지만 server 동작을 뜻하지는 않는다.
- **추론됨**: 관찰값과 통제 조건이 해석을 지지하지만 직접 event가 없는 경우다.
- **관찰되지 않음**: 이번 event surface와 workload에서 확인하지 못한 주장이다.
- **수정 필요**: 같은 버전·설정·도구 범위에서 책 문장과 직접 충돌할 때만 사용한다.
- **추가 관측 필요**: 사건을 유도하지 않았거나 범위가 달라 이번 실행만으로 판단할 수 없다.
- 따라서 **관찰하지 못했다는 사실만으로 책이 틀렸다고 판정하지 않는다.**

## 무효 시도

- 없음

## 같은 증거를 보는 공개 표면

| 표면 | 시각화 관점 |
| --- | --- |
| [Agent Flow · ch19 실행](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch19) | 원인, tool use/result, permission, claim 경계 |
| [Runtime City · ch19 실행](https://nfbs2000.github.io/speaky-PGSimCity/education/claude-sdk-runtime-city/?run=book-sdk-ko-ch19:book-sdk-live-20260804) | OTel 시간축, 실행 zone, Observed/Missing |
| [Pixel Agents · ch19 증거](https://nfbs2000.github.io/speaky-pixel-agents/book-sdk-evidence/?chapter=ch19) | actor와 tool lifecycle 상태 |
| [Hypnagonia · Observation Ordeal](https://nfbs2000.github.io/speaky-hypnagonia/book-sdk-ordeal/?chapter=ch19) | 검증 주장, 선택, 관찰 consequence |

각 표면은 같은 JSON을 무작정 복제한 것이 아니다. Agent Flow는 원인·도구·결과를, Runtime City는
시간축과 실행 구역을, Pixel Agents는 actor 상태를, Observation Ordeal은 주장·선택·결과를
표현한다. 어느 renderer도 raw evidence의 판정을 바꿀 수 없다.

## 공개 경계

- `setting-source-surface-configured`: Python Agent SDK 0.2.128의 SettingSource는 user, project, local이고 ClaudeAgentOptions.setting_sources는 None, 빈 list, 선택 source list를 구분한다. 문서상 None은 CLI 기본 source, []는 filesystem setting 비활성, project는 CLAUDE.md 로드 조건이다.
- `controlled-pair-configured`: 두 attempt는 같은 chapter bytes, probe source hashes, Opus 5, Read 단일 tool, 동일 CLAUDE.md와 evidence marker, provider concurrency 1을 사용하고 setting_sources만 []와 [project]로 나눴다.
- `safe-readonly-prompt-configured`: 프롬프트는 disposable temporary workspace의 읽기 전용 marker 과업으로 제한했고, loaded instruction이 명시하지 않은 경로 탐색과 관련 없는 작업을 금지했다. 보안 우회나 권한 상승은 요청하지 않았다.
- `project-source-caused-pair-difference`: 동일한 임시 파일, tool surface, prompt와 모델에서 의도적으로 바꾼 변수는 setting_sources였고 memoryFiles와 실제 행동이 함께 갈라졌으므로, 이 통제 pair에서는 project source 활성화를 차이의 원인으로 해석한다. 모든 source 조합에 일반화하지 않는다.
- `host-file-presence-is-not-load-proof`: CLAUDE.md가 두 workspace에 모두 존재했다는 사실만으로 load를 확정하면 안 된다. empty pair는 같은 파일이 있어도 memoryFiles가 비어 있었다.
- `model-self-report-is-not-load-proof`: 모델이 지침을 읽었다고 서술하는지 여부는 load 증명이 아니다. current SDK에서는 memoryFiles inventory와 실제 tool/result를 우선해야 한다.
- `init-model-is-not-response-model-proof`: System init.model은 configured model이다. 보안 거부 뒤 lower-model fallback을 놓치지 않으려면 actual model은 AssistantMessage.model의 단일성으로 판정해야 한다.
- `context-usage-model-is-not-response-model-proof`: get_context_usage().model도 context 계산 대상 모델이지 응답 메시지 자체의 모델 증거로 대체하지 않는다.
- `opus-response-is-not-opus-only-provider-run`: 모든 AssistantMessage가 Opus 5였다는 관찰을 provider run 전체가 Opus만 사용했다는 말로 확대하면 안 된다. Result model_usage에는 Haiku 보조 사용량도 있었다.
- `memory-files-is-not-security-boundary`: memoryFiles에 CLAUDE.md가 나타나는 것은 context load 증거이지 permission, hook 또는 sandbox 강제를 뜻하지 않는다. CLAUDE.md는 모델 지침이다.
- `instructions-loaded-is-not-current-hook`: InstructionsLoaded를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다.
- `config-change-is-not-current-hook`: ConfigChange를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다.
- `file-changed-is-not-current-hook`: FileChanged를 Python SDK 0.2.128 HookEvent literal 또는 이번 stream의 실제 event로 가르치면 안 된다.
- `none-and-empty-setting-sources-differ`: setting_sources=None과 []를 같은 것으로 설명하면 안 된다. SDK 0.2.128 문서상 None은 CLI 기본 source를 따르고 []는 filesystem setting을 비활성화한다.
- `otel-is-not-independent-runtime-proof`: OTel은 같은 AttemptRecorder가 raw SDK와 host process event를 투영한 결과다. sequence 정합성은 수집 파이프라인 검증이지 독립 provider 관측이 아니다.
- `full-source-precedence-not-observed`: managed, user, project, local 전체 충돌 우선순위는 이번 [] 대 [project] pair에서 실행하지 않았다. SettingSource literal 자체도 user, project, local 세 값이다.
- `include-resolution-not-observed`: CLAUDE.md의 @include 해석, 누락 파일, 재귀와 순환 규칙은 실행하지 않았다.
- `path-frontmatter-not-observed`: frontmatter paths의 일치 또는 불일치 작업 범위는 실행하지 않았다.
- `html-comment-processing-not-observed`: HTML comment 안팎의 instruction 처리 또는 stripping은 실행하지 않았다.
- `size-budget-not-observed`: 40K 또는 다른 instruction size 경계 전후의 sentinel 보존은 실행하지 않았다.
- `runtime-file-change-not-observed`: 동일 session에서 CLAUDE.md를 수정한 전후의 memoryFiles 또는 행동 갱신은 실행하지 않았다.
- `claude-rules-traversal-not-observed`: .claude/rules 순회와 하위 경로별 rule 적용은 실행하지 않았다.
- `default-none-behavior-not-observed`: SDK 문서에는 setting_sources=None의 기본 동작이 기술돼 있지만 이번 live pair는 None case를 실행하지 않았다.
- `skills-source-interaction-not-observed`: skills option이 setting_sources 기본값과 tool surface를 보강하는 현재 SDK 경로는 이번 skills=None pair에서 인과 실험하지 않았다.
- `precedence-matrix-required`: user, project, local과 별도 managed policy의 충돌 우선순위를 말하려면 source별 상반 marker를 격리한 순차 matrix가 필요하다.
- `instruction-feature-matrix-required`: include, path scope, comment 처리, size budget, .claude/rules는 각 기능을 한 변수씩 바꾼 별도 temp fixture가 필요하다.
- `session-change-tracking-required`: 실행 중 변경 추적은 같은 client session에서 query 사이에 파일을 바꾸고 memoryFiles와 marker를 전후 수집해야 한다.
- `none-and-skills-live-cases-required`: setting_sources=None 기본과 skills auto-configuration은 문서 표면과 실제 memoryFiles/tool listing을 분리해 추가 관찰해야 한다.
- `context-usage-unavailable-path-required`: get_context_usage가 지원되지 않거나 오류를 반환하는 CLI 조합에서 행동 증거로 어떻게 fallback할지는 별도 호환성 case가 필요하다.

- 공개 JSON: [ch19-live.json](./ch19-live.json)
