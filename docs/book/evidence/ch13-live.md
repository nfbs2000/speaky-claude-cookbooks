<!-- Generated by scripts/publish_book_sdk_live_evidence.mjs. -->

# 13장 실제 Python SDK 관찰

이 페이지는 cookbook 예제를 설명하는 정적 해설이 아니라, Python Claude Agent SDK를 실제
`claude-opus-5`로 순차 호출해 저장한 공개 증거다. 테스트 assertion은 recorder와
projection 코드의 계약만 검증하며, 아래 판정은 raw SDK message, permission callback, host process
event와 artifact snapshot 중 이 장이 실제로 발생시킨 사건에서 나왔다.

!!! evidence "한 번 수집하고 여러 표면에서 재생"
    원전은 36개 사건이고 공개 projection은 38개다.
    credential, private prompt, 비공개 추론 payload, 사용자 절대 경로는 공개하지 않았다. 공개 사건은
    source event ID와 SHA-256을 유지한다.

## 실행 신원

| 항목 | 값 |
| --- | --- |
| campaign | `book-sdk-live-20260803` |
| model | `claude-opus-5` |
| proof gate | `accepted_with_notes` |
| secret scan | `passed` |
| raw source / public | **36 / 38** |
| configured / observed / inferred | **1 / 10 / 1** |
| not observed / correction / more evidence | **4 / 5 / 4** |
| trace SHA-256 | `9eb1c8cbd53e77a23c4e3b5f55d19308a9e72966121dd07a9c283412e4250c7e` |
| summary SHA-256 | `026facd48885166ade4c1d48ea3ec9d4fd6eb4700b61be3716dc77166527d00a` |

## 무결성 검증된 실제 실행과 보조 출처

| attempt | source chapter | role | case | actual model | source events | raw SDK SHA-256 | host process SHA-256 |
| --- | --- | --- | --- | --- | ---: | --- | --- |
| `232127-6b9cb9b3` | `ch13` | **replayed** | `stable-prefix-dynamic-suffix` | `claude-opus-5` | 36 | `47a37a15a512131dbc384ba93de2ea93ef31568ced81b3c4647220fb0534a37f` | `93ceb74454c810fc950cffb964ed70903f2a4773c33b0f005ea822cdfa2a7ef1` |

`supporting-only`는 다른 장이 소유한 원본 실행을 이 장의 claim 출처로만 인용한다는 뜻이다.
그 실행은 이 장의 replay timeline에 다시 삽입하거나 복제하지 않는다.

## 관찰 판정

| claim | 판정 | 실제 관찰 또는 경계 | 첫 source ref |
| --- | --- | --- | --- |
| `v2-prefix-and-isolation-configured` | **설정됨** | host는 7,806-character V2 prefix, 서로 다른 A/B suffix, 두 새 SDK client, 빈 tool/settings surface, fallback 없음, provider concurrency 1을 설정했다. | `attempt:232127-6b9cb9b3:manifest:option_summary` |
| `attempt-integrity-files-match` | **관찰됨** | integrity manifest가 열거한 manifest, verdict, raw 4종, OTel SHA-256 7개를 현재 파일에서 다시 계산해 모두 일치했다. | `spec:claims:attempt-integrity-files-match` |
| `four-execution-source-hashes-match` | **관찰됨** | manifest는 실행 당시 chapter, probe, case, runtime source SHA-256 네 개를 묶었고 실행 직후 현재 파일 hash와 4/4 일치했다. | `attempt:232127-6b9cb9b3:manifest:option_summary` |
| `actual-opus-and-serial-boundary` | **관찰됨** | 두 init model은 Opus 5이고 A finish sequence 20 다음에 B start sequence 21이 있어 provider 실행은 병렬이 아니다. | `attempt:232127-6b9cb9b3:sdk-message:2` |
| `two-independent-sdk-sessions` | **관찰됨** | init sequence 2와 22는 서로 다른 session identifier를 보고해 A와 B가 독립 SDK session임을 보였다. | `attempt:232127-6b9cb9b3:sdk-message:2` |
| `run-a-created-fresh-v2-cache` | **관찰됨** | A Result sequence 19는 cache creation 2,570, cache read 0, marker A, terminal success를 직접 기록했다. | `attempt:232127-6b9cb9b3:sdk-message:19` |
| `run-b-reported-cache-read` | **관찰됨** | B Result sequence 33은 cache creation 143, cache read 2,427, marker B, terminal success를 직접 기록했다. | `attempt:232127-6b9cb9b3:sdk-message:33` |
| `dynamic-marker-task-completion-preserved` | **관찰됨** | 서로 다른 suffix를 받은 두 Result는 각각 정확한 A/B marker를 반환했고 두 terminal 상태 모두 success였다. | `attempt:232127-6b9cb9b3:sdk-message:19` |
| `empty-observed-tool-surface` | **관찰됨** | 두 init event의 tools 목록은 모두 비어 있어 이 pair에 model-visible tool surface가 없음을 보였다. | `attempt:232127-6b9cb9b3:sdk-message:2` |
| `raw-and-otel-sdk-sequences-align` | **관찰됨** | raw SDK 30개와 OTel sdk.message 30개 sequence가 순서까지 일치하고 38개 span은 한 trace에 속한다. | `spec:claims:raw-and-otel-sdk-sequences-align` |
| `terminal-model-usage-has-opus-and-haiku` | **관찰됨** | 두 terminal model_usage는 primary Opus 5뿐 아니라 Haiku 4.5의 별도 provider usage도 기록했다. | `attempt:232127-6b9cb9b3:sdk-message:19` |
| `read-tokens-map-to-stable-prefix-mostly` | **추론됨** | B의 read 2,427과 creation 143 합이 A creation 2,570과 같고 host 입력 차이는 suffix뿐이므로 long prefix 대부분의 reuse를 강하게 지지한다. server segment-to-byte map은 없어 attribution은 추론이다. | `attempt:232127-6b9cb9b3:manifest:option_summary` |
| `messages-cache-control-is-not-agent-option` | **범위 수정 필요** | Messages API cookbook의 cache_control을 설치된 Python ClaudeAgentOptions field로 설명하면 안 된다. Agent SDK 실험은 system prompt와 Result usage 표면을 사용했다. | `spec:claims:messages-cache-control-is-not-agent-option` |
| `host-prefix-hash-is-not-full-server-key` | **범위 수정 필요** | host가 기록한 system prompt hash는 통제 입력 증거이지 전체 server cache key 구성 증거가 아니다. | `attempt:232127-6b9cb9b3:manifest:option_summary` |
| `cache-scope-attribution-not-observed` | **관찰되지 않음** | raw SDK와 terminal usage에는 global 또는 org cache scope attribution이 없다. | `spec:claims:cache-scope-attribution-not-observed` |
| `ttl-expiry-boundary-not-observed` | **관찰되지 않음** | usage의 ephemeral_1h field 이름은 관찰했지만 시간 경계를 넘겨 호출하지 않아 실제 만료 시점은 관찰하지 못했다. | `attempt:232127-6b9cb9b3:sdk-message:19` |
| `latency-causality-not-observed` | **관찰되지 않음** | 한 A/B pair는 반복 latency 분포가 아니므로 cache가 일반 latency 개선을 일으켰다는 인과를 관찰하지 못했다. | `spec:claims:latency-causality-not-observed` |
| `mcp-tool-schema-cache-break-not-observed` | **관찰되지 않음** | tool과 MCP surface가 비어 있었으므로 schema 변경이 깨뜨리는 cache segment는 이번 run에서 관찰하지 못했다. | `attempt:232127-6b9cb9b3:sdk-message:2` |
| `primary-opus-is-not-only-provider-usage` | **범위 수정 필요** | primary assistant init은 Opus 5지만 terminal model_usage에는 Haiku 4.5도 있으므로 전체 provider usage를 Opus 하나로 표현하면 안 된다. | `attempt:232127-6b9cb9b3:sdk-message:19` |
| `captured-is-not-cache-claim-pass` | **범위 수정 필요** | OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이지 cache 주장의 자동 pass가 아니다. | `spec:claims:captured-is-not-cache-claim-pass` |
| `otel-is-not-independent-provider-proof` | **범위 수정 필요** | 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 trace를 독립 provider 관측기로 부르면 안 된다. | `spec:claims:otel-is-not-independent-provider-proof` |
| `ttl-boundary-sweep-required` | **추가 관측 필요** | 5분과 1시간 전후의 동일 prefix를 순차 호출해 실제 expiry boundary를 측정하는 별도 장시간 case가 필요하다. | `spec:claims:ttl-boundary-sweep-required` |
| `repeated-latency-cost-sample-required` | **추가 관측 필요** | cache usage와 latency/cost의 분포를 분리하려면 같은 조건의 반복 sequential sample이 필요하다. | `spec:claims:repeated-latency-cost-sample-required` |
| `tool-mcp-schema-ab-required` | **추가 관측 필요** | tool 또는 MCP schema 하나만 바꾸는 A/B에서 cache read segment 변화를 측정해야 schema-break 주장을 검증할 수 있다. | `spec:claims:tool-mcp-schema-ab-required` |
| `preset-variants-required` | **추가 관측 필요** | string system prompt와 preset append/exclude_dynamic_sections의 cache 차이는 별도 controlled case가 필요하다. | `spec:claims:preset-variants-required` |

중요한 판정 규칙은 다음과 같다.

- **관찰됨**: 기대 사건과 순서가 이번 raw evidence에 직접 존재한다.
- **설정됨**: host가 입력과 실행 조건을 고정했지만 server 동작을 뜻하지는 않는다.
- **추론됨**: 관찰값과 통제 조건이 해석을 지지하지만 직접 event가 없는 경우다.
- **관찰되지 않음**: 이번 event surface와 workload에서 확인하지 못한 주장이다.
- **수정 필요**: 같은 버전·설정·도구 범위에서 책 문장과 직접 충돌할 때만 사용한다.
- **추가 관측 필요**: 사건을 유도하지 않았거나 범위가 달라 이번 실행만으로 판단할 수 없다.
- 따라서 **관찰하지 못했다는 사실만으로 책이 틀렸다고 판정하지 않는다.**

## 무효 시도

- `114239-0c0565f2`: run A는 cache creation을 기록했지만 run B가 provider overload retry 뒤 완료되지 않아 pair 증거로 채택하지 않았다. 이 시도는 **invalid attempt**다.

## 같은 증거를 보는 공개 표면

| 표면 | 시각화 관점 |
| --- | --- |
| [Agent Flow · ch13 실행](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch13) | 원인, tool use/result, permission, claim 경계 |
| [Runtime City · ch13 실행](https://nfbs2000.github.io/speaky-PGSimCity/education/claude-sdk-runtime-city/?run=book-sdk-ko-ch13:book-sdk-live-20260803) | OTel 시간축, 실행 zone, Observed/Missing |
| [Pixel Agents · ch13 증거](https://nfbs2000.github.io/speaky-pixel-agents/book-sdk-evidence/?chapter=ch13) | actor와 tool lifecycle 상태 |
| [Hypnagonia · Observation Ordeal](https://nfbs2000.github.io/speaky-hypnagonia/book-sdk-ordeal/?chapter=ch13) | 검증 주장, 선택, 관찰 consequence |

각 표면은 같은 JSON을 무작정 복제한 것이 아니다. Agent Flow는 원인·도구·결과를, Runtime City는
시간축과 실행 구역을, Pixel Agents는 actor 상태를, Observation Ordeal은 주장·선택·결과를
표현한다. 어느 renderer도 raw evidence의 판정을 바꿀 수 없다.

## 공개 경계

- `v2-prefix-and-isolation-configured`: host는 7,806-character V2 prefix, 서로 다른 A/B suffix, 두 새 SDK client, 빈 tool/settings surface, fallback 없음, provider concurrency 1을 설정했다.
- `read-tokens-map-to-stable-prefix-mostly`: B의 read 2,427과 creation 143 합이 A creation 2,570과 같고 host 입력 차이는 suffix뿐이므로 long prefix 대부분의 reuse를 강하게 지지한다. server segment-to-byte map은 없어 attribution은 추론이다.
- `messages-cache-control-is-not-agent-option`: Messages API cookbook의 cache_control을 설치된 Python ClaudeAgentOptions field로 설명하면 안 된다. Agent SDK 실험은 system prompt와 Result usage 표면을 사용했다.
- `host-prefix-hash-is-not-full-server-key`: host가 기록한 system prompt hash는 통제 입력 증거이지 전체 server cache key 구성 증거가 아니다.
- `cache-scope-attribution-not-observed`: raw SDK와 terminal usage에는 global 또는 org cache scope attribution이 없다.
- `ttl-expiry-boundary-not-observed`: usage의 ephemeral_1h field 이름은 관찰했지만 시간 경계를 넘겨 호출하지 않아 실제 만료 시점은 관찰하지 못했다.
- `latency-causality-not-observed`: 한 A/B pair는 반복 latency 분포가 아니므로 cache가 일반 latency 개선을 일으켰다는 인과를 관찰하지 못했다.
- `mcp-tool-schema-cache-break-not-observed`: tool과 MCP surface가 비어 있었으므로 schema 변경이 깨뜨리는 cache segment는 이번 run에서 관찰하지 못했다.
- `primary-opus-is-not-only-provider-usage`: primary assistant init은 Opus 5지만 terminal model_usage에는 Haiku 4.5도 있으므로 전체 provider usage를 Opus 하나로 표현하면 안 된다.
- `captured-is-not-cache-claim-pass`: OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이지 cache 주장의 자동 pass가 아니다.
- `otel-is-not-independent-provider-proof`: 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 trace를 독립 provider 관측기로 부르면 안 된다.
- `ttl-boundary-sweep-required`: 5분과 1시간 전후의 동일 prefix를 순차 호출해 실제 expiry boundary를 측정하는 별도 장시간 case가 필요하다.
- `repeated-latency-cost-sample-required`: cache usage와 latency/cost의 분포를 분리하려면 같은 조건의 반복 sequential sample이 필요하다.
- `tool-mcp-schema-ab-required`: tool 또는 MCP schema 하나만 바꾸는 A/B에서 cache read segment 변화를 측정해야 schema-break 주장을 검증할 수 있다.
- `preset-variants-required`: string system prompt와 preset append/exclude_dynamic_sections의 cache 차이는 별도 controlled case가 필요하다.

- 공개 JSON: [ch13-live.json](./ch13-live.json)
