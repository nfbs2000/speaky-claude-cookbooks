<!-- Generated by scripts/publish_book_sdk_live_evidence.mjs. -->

# 15장 실제 Python SDK 관찰

이 페이지는 cookbook 예제를 설명하는 정적 해설이 아니라, Python Claude Agent SDK를 실제
`claude-opus-5`로 순차 호출해 저장한 공개 증거다. 테스트 assertion은 recorder와
projection 코드의 계약만 검증하며, 아래 판정은 raw SDK message, permission callback, host process
event와 artifact snapshot 중 이 장이 실제로 발생시킨 사건에서 나왔다.

!!! evidence "한 번 수집하고 여러 표면에서 재생"
    원전은 60개 사건이고 공개 projection은 47개다.
    credential, private prompt, 비공개 추론 payload, 사용자 절대 경로는 공개하지 않았다. 공개 사건은
    source event ID와 SHA-256을 유지한다.

## 실행 신원

| 항목 | 값 |
| --- | --- |
| campaign | `book-sdk-live-20260804` |
| model | `claude-opus-5` |
| proof gate | `accepted_with_notes` |
| secret scan | `passed` |
| raw source / public | **60 / 47** |
| configured / observed / inferred | **1 / 12 / 2** |
| not observed / correction / more evidence | **5 / 6 / 3** |
| trace SHA-256 | `7264443f4a7983d84ea35e256978212111386b07ee77c78a67291c3652538598` |
| summary SHA-256 | `2c600fc072aa606064ba3a3d6b363d7bb0e2bdd858d6888eafc6c94e64f59f0b` |

## 무결성 검증된 실제 실행과 보조 출처

| attempt | source chapter | role | case | actual model | source events | raw SDK SHA-256 | host process SHA-256 |
| --- | --- | --- | --- | --- | ---: | --- | --- |
| `002934-a0095d45` | `ch15` | **replayed** | `volatile-timestamp-prefix` | `claude-opus-5` | 60 | `2a941b0f2ef9dd786bc094b764e4f6a1d213f37a869fd64809f23543b6b66ae0` | `f06228539cdb81a2271681c83d8b8400177be7c618362063f14747ec7c3feeeb` |

`supporting-only`는 다른 장이 소유한 원본 실행을 이 장의 claim 출처로만 인용한다는 뜻이다.
그 실행은 이 장의 replay timeline에 다시 삽입하거나 복제하지 않는다.

## 관찰 판정

| claim | 판정 | 실제 관찰 또는 경계 | 첫 source ref |
| --- | --- | --- | --- |
| `v2-prefix-triple-configured` | **설정됨** | Host는 fresh attempt nonce를 공유하는 8,190-character system prompt 세 개를 만들고 stable-a/control은 byte-identical, volatile-b는 index 41 timestamp 한 글자만 다르게 설정했다. 새 SDK client/config directory 세 개를 concurrency 1로 실행했다. | `attempt:002934-a0095d45:manifest:option_summary` |
| `four-execution-source-hashes-match` | **관찰됨** | Manifest가 실행 당시 chapter, probe, cases, runtime source SHA-256 네 개를 묶었고 실행 직후 현재 실행 파일과 4/4 일치했다. | `spec:claims:four-execution-source-hashes-match` |
| `actual-opus-and-serial-boundaries` | **관찰됨** | 세 init model은 Opus 5이고 stable-a finish 20 뒤 control start 21, control finish 40 뒤 volatile-b start 41이 이어져 provider 실행이 병렬이 아님을 보였다. | `attempt:002934-a0095d45:sdk-message:2` |
| `three-independent-sdk-sessions` | **관찰됨** | Init sequence 2, 22, 42가 서로 다른 runtime session ID를 보고해 세 run이 독립 SDK session임을 보였다. | `attempt:002934-a0095d45:sdk-message:2` |
| `host-system-hash-relations` | **관찰됨** | Process markers의 stable-a/control system SHA-256은 같고 volatile-b만 달라 내부 통제군과 변경군의 입력 관계가 기록됐다. | `attempt:002934-a0095d45:process-event:1` |
| `stable-baseline-created-cache` | **관찰됨** | Stable-a Result sequence 19는 cache creation 2,755, cache read 0, exact marker와 terminal success를 기록했다. | `attempt:002934-a0095d45:sdk-message:19` |
| `byte-identical-control-read-cache` | **관찰됨** | Byte-identical stable-control Result sequence 39는 cache creation 130, cache read 2,629, exact marker와 terminal success를 기록했다. | `attempt:002934-a0095d45:sdk-message:39` |
| `volatile-prefix-created-cache` | **관찰됨** | 앞쪽 timestamp 한 글자를 바꾼 volatile-b Result sequence 57은 cache creation 2,757, cache read 0, exact marker와 terminal success를 기록했다. | `attempt:002934-a0095d45:sdk-message:57` |
| `all-markers-and-boundaries-complete` | **관찰됨** | 세 Result는 subtype success, is_error false, 요청 marker를 기록했고 finish marker는 18, 18, 16 SDK messages와 outcome completed를 보존했다. | `attempt:002934-a0095d45:sdk-message:19` |
| `init-runtime-surfaces-preserved` | **관찰됨** | 세 init은 tools/MCP/plugins 0개와 동시에 같은 built-in agents 5개, skills 16개, slash commands 43개를 보고했다. | `attempt:002934-a0095d45:sdk-message:2` |
| `primary-opus-with-secondary-model-usage` | **관찰됨** | Primary init model은 Opus 5지만 세 terminal model_usage에는 Haiku 4.5 사용량도 함께 기록됐다. | `attempt:002934-a0095d45:sdk-message:19` |
| `raw-and-otel-sequences-align` | **관찰됨** | Raw SDK 52개와 OTel sdk.message 52개, raw process 8개와 OTel process.event 8개의 sequence가 각각 순서까지 일치하고 총 62 span은 한 trace에 속한다. | `spec:claims:raw-and-otel-sequences-align` |
| `attempt-integrity-files-match` | **관찰됨** | Integrity manifest가 열거한 manifest, verdict, raw 4종, OTel SHA-256 7개를 현재 파일에서 다시 계산해 모두 일치했다. | `spec:claims:attempt-integrity-files-match` |
| `timestamp-change-is-strong-miss-candidate` | **추론됨** | 같은 시도 안에서 byte-identical control은 2,629 read를 기록하고 index 41 한 글자 변경군은 read 0을 기록했으므로 앞쪽 timestamp 변경이 이 표본의 miss를 설명하는 가장 강한 통제 후보다. Server cause event는 없어 인과 확정은 아니다. | `attempt:002934-a0095d45:manifest:option_summary` |
| `control-read-mostly-maps-to-stable-prefix` | **추론됨** | Control의 read 2,629와 create 130은 baseline create 2,755와 비슷한 총량이고 host 차이는 latest user marker뿐이어서 안정 prefix 대부분의 재사용을 지지한다. Provider token-to-byte map은 없어 attribution은 추론이다. | `attempt:002934-a0095d45:sdk-message:19` |
| `messages-cache-control-and-ttft-are-not-agent-options` | **범위 수정 필요** | Messages API cookbook의 cache_control과 stream TTFT를 ClaudeAgentOptions 기능으로 설명하면 안 된다. 이 Agent SDK probe는 Result usage를 수집했지만 provider cache-attributed TTFT를 수집하지 않았다. | `spec:claims:messages-cache-control-and-ttft-are-not-agent-options` |
| `empty-settings-do-not-remove-builtins` | **범위 수정 필요** | setting_sources와 tools를 비웠다는 사실을 built-in agent, skill, slash command도 없다는 뜻으로 설명하면 안 된다. 실제 init에는 세 표면이 존재했다. | `attempt:002934-a0095d45:sdk-message:2` |
| `captured-is-not-cache-pass` | **범위 수정 필요** | OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이지 cache 주장의 자동 pass가 아니다. | `spec:claims:captured-is-not-cache-pass` |
| `primary-model-is-not-only-provider-usage` | **범위 수정 필요** | 세 init의 primary model이 Opus 5여도 terminal model_usage에 Haiku 4.5가 있으므로 전체 provider usage를 Opus 하나로 표현하면 안 된다. | `attempt:002934-a0095d45:sdk-message:19` |
| `otel-is-not-independent-provider-proof` | **범위 수정 필요** | 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 sequence 일치는 recorder 무손실 확인이지 독립 provider 관측 증거가 아니다. | `spec:claims:otel-is-not-independent-provider-proof` |
| `one-triple-is-not-general-cost-speed-quality-proof` | **범위 수정 필요** | 한 controlled triple의 실제 usage와 cost 차이만으로 일반 비용 절감률, 속도 향상, 결과 품질 변화를 증명했다고 쓰면 안 된다. | `spec:claims:one-triple-is-not-general-cost-speed-quality-proof` |
| `provider-cache-key-and-breakpoint-not-observed` | **관찰되지 않음** | Provider cache key, breakpoint, eviction reason과 system byte가 cache token segment에 대응하는 방식은 SDK event에서 관찰되지 않았다. | `spec:claims:provider-cache-key-and-breakpoint-not-observed` |
| `provider-ttft-and-ttl-not-observed` | **관찰되지 않음** | 이 Agent SDK probe는 provider cache-attributed TTFT와 실제 TTL 만료 경계를 관찰하지 않았다. | `spec:claims:provider-ttft-and-ttl-not-observed` |
| `dynamic-list-cache-effect-not-observed` | **관찰되지 않음** | Init에서 agent, skill, slash command 목록을 보았지만 세 run 사이에서 위치나 크기를 바꾸지 않았으므로 해당 목록의 cache 효과는 관찰하지 않았다. | `attempt:002934-a0095d45:sdk-message:2` |
| `path-skill-budget-conditional-relocation-not-observed` | **관찰되지 않음** | Absolute path placeholder, skill/document budget, conditional paragraph prefix/suffix 위치의 효과는 이번 timestamp case에서 실행하지 않았다. | `spec:claims:path-skill-budget-conditional-relocation-not-observed` |
| `quality-and-repeat-distribution-not-observed` | **관찰되지 않음** | Exact marker 과제는 일반 결과 품질 비교가 아니며 한 triple은 latency, cost, hit-rate의 반복 분포를 만들기에 부족하다. | `spec:claims:quality-and-repeat-distribution-not-observed` |
| `candidate-pattern-pairs-required` | **추가 관측 필요** | Path raw/placeholder, skill budget, conditional paragraph prefix/suffix를 각각 한 변수만 바꾸는 별도 sequential pair가 필요하다. | `spec:claims:candidate-pattern-pairs-required` |
| `repeated-sequential-samples-required` | **추가 관측 필요** | Cache 패턴의 안정성을 말하려면 fresh nonce를 사용하는 같은 triple을 더 반복하되 provider concurrency 1을 유지해야 한다. | `spec:claims:repeated-sequential-samples-required` |
| `messages-api-ttft-and-ttl-case-required` | **추가 관측 필요** | Explicit cache_control, streaming TTFT, 실제 TTL을 가르치려면 Agent SDK 실험과 섞지 않고 Messages API cookbook 계열의 별도 실제 실행이 필요하다. | `spec:claims:messages-api-ttft-and-ttl-case-required` |

중요한 판정 규칙은 다음과 같다.

- **관찰됨**: 기대 사건과 순서가 이번 raw evidence에 직접 존재한다.
- **설정됨**: host가 입력과 실행 조건을 고정했지만 server 동작을 뜻하지는 않는다.
- **추론됨**: 관찰값과 통제 조건이 해석을 지지하지만 직접 event가 없는 경우다.
- **관찰되지 않음**: 이번 event surface와 workload에서 확인하지 못한 주장이다.
- **수정 필요**: 같은 버전·설정·도구 범위에서 책 문장과 직접 충돌할 때만 사용한다.
- **추가 관측 필요**: 사건을 유도하지 않았거나 범위가 달라 이번 실행만으로 판단할 수 없다.
- 따라서 **관찰하지 못했다는 사실만으로 책이 틀렸다고 판정하지 않는다.**

## 무효 시도

- 없음

## 같은 증거를 보는 공개 표면

| 표면 | 시각화 관점 |
| --- | --- |
| [Agent Flow · ch15 실행](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch15) | 원인, tool use/result, permission, claim 경계 |
| [Runtime City · ch15 실행](https://nfbs2000.github.io/speaky-PGSimCity/education/claude-sdk-runtime-city/?run=book-sdk-ko-ch15:book-sdk-live-20260804) | OTel 시간축, 실행 zone, Observed/Missing |
| [Pixel Agents · ch15 증거](https://nfbs2000.github.io/speaky-pixel-agents/book-sdk-evidence/?chapter=ch15) | actor와 tool lifecycle 상태 |
| [Hypnagonia · Observation Ordeal](https://nfbs2000.github.io/speaky-hypnagonia/book-sdk-ordeal/?chapter=ch15) | 검증 주장, 선택, 관찰 consequence |

각 표면은 같은 JSON을 무작정 복제한 것이 아니다. Agent Flow는 원인·도구·결과를, Runtime City는
시간축과 실행 구역을, Pixel Agents는 actor 상태를, Observation Ordeal은 주장·선택·결과를
표현한다. 어느 renderer도 raw evidence의 판정을 바꿀 수 없다.

## 공개 경계

- `v2-prefix-triple-configured`: Host는 fresh attempt nonce를 공유하는 8,190-character system prompt 세 개를 만들고 stable-a/control은 byte-identical, volatile-b는 index 41 timestamp 한 글자만 다르게 설정했다. 새 SDK client/config directory 세 개를 concurrency 1로 실행했다.
- `timestamp-change-is-strong-miss-candidate`: 같은 시도 안에서 byte-identical control은 2,629 read를 기록하고 index 41 한 글자 변경군은 read 0을 기록했으므로 앞쪽 timestamp 변경이 이 표본의 miss를 설명하는 가장 강한 통제 후보다. Server cause event는 없어 인과 확정은 아니다.
- `control-read-mostly-maps-to-stable-prefix`: Control의 read 2,629와 create 130은 baseline create 2,755와 비슷한 총량이고 host 차이는 latest user marker뿐이어서 안정 prefix 대부분의 재사용을 지지한다. Provider token-to-byte map은 없어 attribution은 추론이다.
- `messages-cache-control-and-ttft-are-not-agent-options`: Messages API cookbook의 cache_control과 stream TTFT를 ClaudeAgentOptions 기능으로 설명하면 안 된다. 이 Agent SDK probe는 Result usage를 수집했지만 provider cache-attributed TTFT를 수집하지 않았다.
- `empty-settings-do-not-remove-builtins`: setting_sources와 tools를 비웠다는 사실을 built-in agent, skill, slash command도 없다는 뜻으로 설명하면 안 된다. 실제 init에는 세 표면이 존재했다.
- `captured-is-not-cache-pass`: OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이지 cache 주장의 자동 pass가 아니다.
- `primary-model-is-not-only-provider-usage`: 세 init의 primary model이 Opus 5여도 terminal model_usage에 Haiku 4.5가 있으므로 전체 provider usage를 Opus 하나로 표현하면 안 된다.
- `otel-is-not-independent-provider-proof`: 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 sequence 일치는 recorder 무손실 확인이지 독립 provider 관측 증거가 아니다.
- `one-triple-is-not-general-cost-speed-quality-proof`: 한 controlled triple의 실제 usage와 cost 차이만으로 일반 비용 절감률, 속도 향상, 결과 품질 변화를 증명했다고 쓰면 안 된다.
- `provider-cache-key-and-breakpoint-not-observed`: Provider cache key, breakpoint, eviction reason과 system byte가 cache token segment에 대응하는 방식은 SDK event에서 관찰되지 않았다.
- `provider-ttft-and-ttl-not-observed`: 이 Agent SDK probe는 provider cache-attributed TTFT와 실제 TTL 만료 경계를 관찰하지 않았다.
- `dynamic-list-cache-effect-not-observed`: Init에서 agent, skill, slash command 목록을 보았지만 세 run 사이에서 위치나 크기를 바꾸지 않았으므로 해당 목록의 cache 효과는 관찰하지 않았다.
- `path-skill-budget-conditional-relocation-not-observed`: Absolute path placeholder, skill/document budget, conditional paragraph prefix/suffix 위치의 효과는 이번 timestamp case에서 실행하지 않았다.
- `quality-and-repeat-distribution-not-observed`: Exact marker 과제는 일반 결과 품질 비교가 아니며 한 triple은 latency, cost, hit-rate의 반복 분포를 만들기에 부족하다.
- `candidate-pattern-pairs-required`: Path raw/placeholder, skill budget, conditional paragraph prefix/suffix를 각각 한 변수만 바꾸는 별도 sequential pair가 필요하다.
- `repeated-sequential-samples-required`: Cache 패턴의 안정성을 말하려면 fresh nonce를 사용하는 같은 triple을 더 반복하되 provider concurrency 1을 유지해야 한다.
- `messages-api-ttft-and-ttl-case-required`: Explicit cache_control, streaming TTFT, 실제 TTL을 가르치려면 Agent SDK 실험과 섞지 않고 Messages API cookbook 계열의 별도 실제 실행이 필요하다.

- 공개 JSON: [ch15-live.json](./ch15-live.json)
