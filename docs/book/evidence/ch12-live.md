<!-- Generated by scripts/publish_book_sdk_live_evidence.mjs. -->

# 12장 실제 Python SDK 관찰

이 페이지는 cookbook 예제를 설명하는 정적 해설이 아니라, Python Claude Agent SDK를 실제
`claude-opus-5`로 순차 호출해 저장한 공개 증거다. 테스트 assertion은 recorder와
projection 코드의 계약만 검증하며, 아래 판정은 raw SDK message, permission callback, host process
event와 artifact snapshot 중 이 장이 실제로 발생시킨 사건에서 나왔다.

!!! evidence "한 번 수집하고 여러 표면에서 재생"
    원전은 124개 사건이고 공개 projection은 51개다.
    credential, private prompt, 비공개 추론 payload, 사용자 절대 경로는 공개하지 않았다. 공개 사건은
    source event ID와 SHA-256을 유지한다.

## 실행 신원

| 항목 | 값 |
| --- | --- |
| campaign | `book-sdk-live-20260803` |
| model | `claude-opus-5` |
| proof gate | `accepted_with_notes` |
| secret scan | `passed` |
| raw source / public | **124 / 51** |
| observed / correction / more evidence | **10 / 14 / 6** |
| trace SHA-256 | `76a7fb8ca0aaac67d86f30846890381a292056769f8ad5bb9821623e6b2e3ee8` |
| summary SHA-256 | `38eb9f16fd714b9ef266aa09c0cec668a150b037ce78c9b325c6283b24e81391` |

## 무결성 검증된 실제 실행과 보조 출처

| attempt | source chapter | role | case | actual model | source events | raw SDK SHA-256 | host process SHA-256 |
| --- | --- | --- | --- | --- | ---: | --- | --- |
| `221711-cf0efe19` | `ch12` | **replayed** | `inline-over-50k` | `claude-opus-5` | 68 | `61557f93987e3955eed6482df23ecdef772191ccf39b3dba9f7ad33e684ad62e` | `c30480060c15038845a52be1b8a124d463b3631c31cb7f61d9876f14e5e43ecc` |
| `221742-59006bcd` | `ch12` | **replayed** | `host-persisted-preview` | `claude-opus-5` | 56 | `d9c4d11af32185ddbbdf9ea5ab27313a41e461d9c7293c2d1e9b9677c815a949` | `7c51610c7f53fd6f1c558ce054cae72abcf72ea64e8a261485524009e620ebe4` |

`supporting-only`는 다른 장이 소유한 원본 실행을 이 장의 claim 출처로만 인용한다는 뜻이다.
그 실행은 이 장의 replay timeline에 다시 삽입하거나 복제하지 않는다.

## 관찰 판정

| claim | 판정 | 실제 관찰 또는 경계 | 첫 source ref |
| --- | --- | --- | --- |
| `attempt-integrity-files-match` | **관찰됨** | 두 accepted attempt의 integrity manifest가 열거한 manifest, verdict, raw 4종, OTel, artifact SHA-256 각 8개를 현재 파일에서 다시 계산해 모두 일치했다. | `spec:claims:attempt-integrity-files-match` |
| `actual-opus-and-serial-execution` | **관찰됨** | 두 init과 terminal capture는 actual model Opus 5를 기록했고 manifest는 provider concurrency 1을 기록했다. 첫 실행 종료 뒤 두 번째 실행을 시작했다. | `attempt:221711-cf0efe19:manifest:option_summary` |
| `four-source-hashes-bound-at-execution` | **관찰됨** | 두 manifest는 실행 당시 chapter, probe, case, runtime source SHA-256 네 개를 묶었고 실행 직후 파일 hash와 일치했다. | `attempt:221711-cf0efe19:manifest:option_summary` |
| `same-controlled-full-output` | **관찰됨** | 두 case의 accepted artifact는 60,082 bytes/characters, 같은 head/tail marker, 같은 content SHA-256을 가진다. | `attempt:221711-cf0efe19:process-event:66` |
| `runtime-overflow-chain` | **관찰됨** | runtime case는 ToolUse 15, handler process 16, 1,587-character replacement ToolResult 20, terminal success 65로 이어진다. | `attempt:221711-cf0efe19:sdk-message:15` |
| `runtime-spill-read-back` | **관찰됨** | capture host는 정리 전에 runtime이 보고한 file을 열어 size, marker, hash를 검증하고 content-addressed evidence copy로 보존했다. | `attempt:221711-cf0efe19:process-event:66` |
| `host-persisted-preview-chain` | **관찰됨** | host case는 persist process 15, handler process 16, 실제 model-visible 340-character ToolResult 18, terminal success 54로 이어진다. | `attempt:221742-59006bcd:process-event:15` |
| `error-text-flag-and-terminal-state-differ` | **관찰됨** | runtime ToolResult text는 Error로 시작하지만 block is_error는 null이고 terminal Result는 success, is_error false, completed다. | `attempt:221711-cf0efe19:sdk-message:20` |
| `token-surfaces-recorded` | **관찰됨** | 두 run은 stream thinking estimate, terminal API usage, Opus 5와 Haiku 4.5의 model-indexed provider usage를 별도 필드로 기록했다. | `attempt:221711-cf0efe19:sdk-message:24` |
| `raw-and-otel-sequences-align` | **관찰됨** | runtime raw SDK 64개와 host raw SDK 52개는 각 OTel sdk.message sequence와 정확히 일치하고 두 trace는 71/59 span을 가진다. | `spec:claims:raw-and-otel-sequences-align` |
| `single-point-is-not-exact-50k-boundary` | **범위 수정 필요** | 60,082-character 한 지점의 외부화는 exact 50,000-character threshold를 확정하지 않는다. | `attempt:221711-cf0efe19:process-event:16` |
| `aggregate-200k-limit-not-observed` | **범위 수정 필요** | 같은 turn에서 여러 tool result를 합산하는 workload를 실행하지 않았으므로 200K aggregate limit을 관찰 사실로 쓸 수 없다. | `spec:claims:aggregate-200k-limit-not-observed` |
| `characters-bytes-repr-and-tokens-differ` | **범위 수정 필요** | 이번 ASCII artifact의 bytes와 characters가 같아도 character, byte, Python representation, API token은 서로 다른 단위다. host ToolResult는 실제 text 340, representation 376이다. | `attempt:221711-cf0efe19:process-event:66` |
| `sdk-files-persisted-event-is-not-current-python-type` | **범위 수정 필요** | Python Agent SDK 0.2.128은 SDKFilesPersistedEvent를 export하지 않고 두 run에도 전용 persisted SystemMessage가 없다. | `spec:claims:sdk-files-persisted-event-is-not-current-python-type` |
| `overflow-text-is-not-run-failure` | **범위 수정 필요** | overflow notice의 Error prefix를 ToolResult error flag 또는 terminal run failure와 합치면 안 된다. | `attempt:221711-cf0efe19:sdk-message:20` |
| `model-attribution-is-not-host-ownership-proof` | **범위 수정 필요** | runtime final의 host tool policy attribution은 handler가 write하지 않았다는 controlled process evidence와 충돌하므로 소유권 증거로 쓰지 않는다. | `attempt:221711-cf0efe19:process-event:16` |
| `exact-internal-writer-is-not-observed` | **범위 수정 필요** | controlled handler 뒤와 model ToolResult 앞의 pipeline 외부화는 관찰했지만 전용 event가 없어 숨은 내부 writer component는 특정하지 않는다. | `attempt:221711-cf0efe19:process-event:16` |
| `host-self-report-is-not-disk-proof` | **범위 수정 필요** | preview의 path/hash 자기보고가 아니라 host process event와 capture-time disk read-back이 persistence를 증명한다. | `attempt:221742-59006bcd:process-event:15` |
| `thinking-estimate-is-not-terminal-usage` | **범위 수정 필요** | thinking_tokens의 estimated_tokens는 stream 추정 신호이며 terminal API input/output usage나 billing count가 아니다. | `attempt:221711-cf0efe19:sdk-message:30` |
| `primary-opus-is-not-only-provider-usage` | **범위 수정 필요** | primary assistant model은 Opus 5지만 두 terminal model_usage에는 Haiku 4.5도 있어 전체 provider usage를 Opus 하나로 쓰면 안 된다. | `attempt:221711-cf0efe19:sdk-message:65` |
| `two-run-usage-difference-is-not-general-savings` | **범위 수정 필요** | 두 independent run의 usage 차이에는 answer 길이와 cache 상태가 섞여 있어 persisted-preview의 일반 비용 절감률로 확대할 수 없다. | `attempt:221711-cf0efe19:sdk-message:65` |
| `captured-is-not-claim-pass` | **범위 수정 필요** | OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이며 threshold claim pass가 아니다. | `spec:claims:captured-is-not-claim-pass` |
| `otel-is-not-independent-provider-proof` | **범위 수정 필요** | 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 OTel을 독립 provider 관측기로 부르면 안 된다. | `spec:claims:otel-is-not-independent-provider-proof` |
| `public-projection-must-redact-runtime-identifiers` | **범위 수정 필요** | runtime overflow text의 로컬 절대경로와 session identifier는 private read-back에만 필요하며 공개 projection에는 content hash와 redacted label만 남긴다. | `spec:claims:public-projection-must-redact-runtime-identifiers` |
| `exact-boundary-sweep-not-observed` | **추가 관측 필요** | 49K/50K/51K와 서로 다른 token density payload를 순차 실행해 exact boundary와 governing unit를 찾아야 한다. | `spec:claims:exact-boundary-sweep-not-observed` |
| `aggregate-budget-pressure-not-observed` | **추가 관측 필요** | 같은 turn의 여러 tool result가 만드는 aggregate budget pressure는 별도 controlled workload가 필요하다. | `spec:claims:aggregate-budget-pressure-not-observed` |
| `runtime-spill-lifecycle-not-observed` | **추가 관측 필요** | runtime 원본 spill file이 session 또는 process cleanup 뒤 얼마나 유지되는지는 관찰하지 않았다. | `spec:claims:runtime-spill-lifecycle-not-observed` |
| `messages-api-count-token-comparison-not-observed` | **추가 관측 필요** | cookbook의 Messages API count_tokens와 Agent SDK runtime usage를 같은 실행 계약으로 비교하는 별도 API-family 실험이 필요하다. | `spec:claims:messages-api-count-token-comparison-not-observed` |
| `repeated-cost-latency-distribution-not-observed` | **추가 관측 필요** | 한 쌍의 run은 benchmark가 아니므로 preview 정책의 cost와 latency 분포는 반복 표본이 필요하다. | `spec:claims:repeated-cost-latency-distribution-not-observed` |
| `unicode-unit-separation-not-observed` | **추가 관측 필요** | Unicode payload로 byte, character, serialized representation, token 단위가 갈라지는 실행을 추가해야 한다. | `spec:claims:unicode-unit-separation-not-observed` |

중요한 판정 규칙은 다음과 같다.

- **관찰됨**: 기대 사건과 순서가 이번 raw evidence에 직접 존재한다.
- **수정 필요**: 같은 버전·설정·도구 범위에서 책 문장과 직접 충돌할 때만 사용한다.
- **추가 관측 필요**: 사건을 유도하지 않았거나 범위가 달라 이번 실행만으로 판단할 수 없다.
- 따라서 **관찰하지 못했다는 사실만으로 책이 틀렸다고 판정하지 않는다.**

## 무효 시도

- `113006-18fd9917`: overflow notice는 관찰했지만 runtime spill file을 임시 workspace 정리 전에 읽지 못해 persistence artifact 증거로 채택하지 않았다. 이 시도는 **invalid attempt**다.
- `113303-a2d4890a`: host file은 관찰했지만 evidence copy가 없었다. 실제 529 api_retry 세 건은 보존하되 CH12 primary artifact proof에서는 제외했다. 이 시도는 **invalid attempt**다.

## 같은 증거를 보는 공개 표면

| 표면 | 시각화 관점 |
| --- | --- |
| [Agent Flow · ch12 실행](https://nfbs2000.github.io/speaky-agent-flow/education/?collection=book-sdk-ko&run=ch12) | 원인, tool use/result, permission, claim 경계 |
| [Runtime City · ch12 실행](https://nfbs2000.github.io/speaky-PGSimCity/education/claude-sdk-runtime-city/?run=book-sdk-ko-ch12:book-sdk-live-20260803) | OTel 시간축, 실행 zone, Observed/Missing |
| [Pixel Agents · ch12 증거](https://nfbs2000.github.io/speaky-pixel-agents/book-sdk-evidence/?chapter=ch12) | actor와 tool lifecycle 상태 |
| [Hypnagonia · Observation Ordeal](https://nfbs2000.github.io/speaky-hypnagonia/book-sdk-ordeal/?chapter=ch12) | 검증 주장, 선택, 관찰 consequence |

각 표면은 같은 JSON을 무작정 복제한 것이 아니다. Agent Flow는 원인·도구·결과를, Runtime City는
시간축과 실행 구역을, Pixel Agents는 actor 상태를, Observation Ordeal은 주장·선택·결과를
표현한다. 어느 renderer도 raw evidence의 판정을 바꿀 수 없다.

## 공개 경계

- `single-point-is-not-exact-50k-boundary`: 60,082-character 한 지점의 외부화는 exact 50,000-character threshold를 확정하지 않는다.
- `aggregate-200k-limit-not-observed`: 같은 turn에서 여러 tool result를 합산하는 workload를 실행하지 않았으므로 200K aggregate limit을 관찰 사실로 쓸 수 없다.
- `characters-bytes-repr-and-tokens-differ`: 이번 ASCII artifact의 bytes와 characters가 같아도 character, byte, Python representation, API token은 서로 다른 단위다. host ToolResult는 실제 text 340, representation 376이다.
- `sdk-files-persisted-event-is-not-current-python-type`: Python Agent SDK 0.2.128은 SDKFilesPersistedEvent를 export하지 않고 두 run에도 전용 persisted SystemMessage가 없다.
- `overflow-text-is-not-run-failure`: overflow notice의 Error prefix를 ToolResult error flag 또는 terminal run failure와 합치면 안 된다.
- `model-attribution-is-not-host-ownership-proof`: runtime final의 host tool policy attribution은 handler가 write하지 않았다는 controlled process evidence와 충돌하므로 소유권 증거로 쓰지 않는다.
- `exact-internal-writer-is-not-observed`: controlled handler 뒤와 model ToolResult 앞의 pipeline 외부화는 관찰했지만 전용 event가 없어 숨은 내부 writer component는 특정하지 않는다.
- `host-self-report-is-not-disk-proof`: preview의 path/hash 자기보고가 아니라 host process event와 capture-time disk read-back이 persistence를 증명한다.
- `thinking-estimate-is-not-terminal-usage`: thinking_tokens의 estimated_tokens는 stream 추정 신호이며 terminal API input/output usage나 billing count가 아니다.
- `primary-opus-is-not-only-provider-usage`: primary assistant model은 Opus 5지만 두 terminal model_usage에는 Haiku 4.5도 있어 전체 provider usage를 Opus 하나로 쓰면 안 된다.
- `two-run-usage-difference-is-not-general-savings`: 두 independent run의 usage 차이에는 answer 길이와 cache 상태가 섞여 있어 persisted-preview의 일반 비용 절감률로 확대할 수 없다.
- `captured-is-not-claim-pass`: OTel CAPTURED, assertion count 0, verdict TODO, passed null은 raw 수집 완료와 수동 판독 대기 상태이며 threshold claim pass가 아니다.
- `otel-is-not-independent-provider-proof`: 같은 recorder가 raw SDK와 process event를 OTel로 투영했으므로 OTel을 독립 provider 관측기로 부르면 안 된다.
- `public-projection-must-redact-runtime-identifiers`: runtime overflow text의 로컬 절대경로와 session identifier는 private read-back에만 필요하며 공개 projection에는 content hash와 redacted label만 남긴다.
- `exact-boundary-sweep-not-observed`: 49K/50K/51K와 서로 다른 token density payload를 순차 실행해 exact boundary와 governing unit를 찾아야 한다.
- `aggregate-budget-pressure-not-observed`: 같은 turn의 여러 tool result가 만드는 aggregate budget pressure는 별도 controlled workload가 필요하다.
- `runtime-spill-lifecycle-not-observed`: runtime 원본 spill file이 session 또는 process cleanup 뒤 얼마나 유지되는지는 관찰하지 않았다.
- `messages-api-count-token-comparison-not-observed`: cookbook의 Messages API count_tokens와 Agent SDK runtime usage를 같은 실행 계약으로 비교하는 별도 API-family 실험이 필요하다.
- `repeated-cost-latency-distribution-not-observed`: 한 쌍의 run은 benchmark가 아니므로 preview 정책의 cost와 latency 분포는 반복 표본이 필요하다.
- `unicode-unit-separation-not-observed`: Unicode payload로 byte, character, serialized representation, token 단위가 갈라지는 실행을 추가해야 한다.

- 공개 JSON: [ch12-live.json](./ch12-live.json)
